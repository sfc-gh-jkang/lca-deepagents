[For translation, open lesson in new tab and use Chrome translate](https://langchain-ai.github.io/lca-lessons/deepagents/m1/m1.5-tools.html)

<style>@import url('../shared/sd-components.css');</style>
<script src="../shared/sd-components.js"></script>

<div class="page-lang-switch" data-page-lang-switch>
  <button class="page-lang-btn" data-page-lang="python" aria-selected="false">Python</button>
  <button class="page-lang-btn" data-page-lang="typescript" aria-selected="false">TypeScript</button>
</div>

# Tools

<style>
.lt-bar {
  display: flex;
  flex-wrap: wrap;
  gap: 20px;
  margin: 28px 0 0;
  border-bottom: 2px solid #CCE9FF;
}
.lt-group { display: flex; gap: 3px; }
.lt-quiz  { --c: #7C3AED; }
.lt-tools { --c: #0369A1; }
.lt-wrap  { --c: #B45309; }
.lt-homework { --c: #0E9F6E; }
.lt-tab {
  font: 500 14px 'IBM Plex Mono', monospace;
  padding: 9px 14px;
  border: none;
  background: transparent;
  color: #40668D;
  cursor: pointer;
  border-bottom: 3px solid transparent;
  margin-bottom: -2px;
  border-radius: 6px 6px 0 0;
  transition: background .15s, color .15s, border-color .15s;
  white-space: nowrap;
}
.lt-tab:hover { background: #F2FAFF; color: #030710; }
.lt-tab.active {
  color: var(--c);
  border-bottom-color: var(--c);
  background: #fff;
}
.lt-panel { display: none; padding-top: 24px; }
.lt-panel.active { display: block; }
@media (max-width: 600px) {
  .lt-bar { flex-wrap: nowrap; overflow-x: auto; gap: 12px; }
  .lt-tab { padding: 8px 10px; font-size: 13px; }
}
</style>

<div class="lt-bar" role="tablist" aria-label="Lesson sections">
  <div class="lt-group lt-tools">
    <button class="lt-tab active" data-p="tools" role="tab" aria-selected="true">Tools</button>
  </div>
  <div class="lt-group lt-wrap">
    <button class="lt-tab" data-p="lab3" role="tab" aria-selected="false">Lab</button>
  </div>
  <div class="lt-group lt-quiz">
    <button class="lt-tab" data-p="quiz" role="tab" aria-selected="false">Quiz</button>
  </div>
  <div class="lt-group lt-homework">
    <button class="lt-tab" data-p="homework" role="tab" aria-selected="false">Homework</button>
  </div>
</div>

<div class="lt-panel active" id="p-tools" markdown="1" role="tabpanel">

<div data-lang="python" markdown="1">

<details style="border:2.5px solid #000;border-radius:6px;background:#fff;margin:1rem 0;"><summary style="padding:10px 16px;cursor:pointer;font-weight:500;font-family:'IBM Plex Mono',monospace;font-size:14px;">Lesson Video Walkthrough</summary><div style="padding:12px 16px 16px;"><div class="video-container" style="max-width:750px;"><div class="video-wrapper"><iframe src="https://share.descript.com/embed/E2a0vp8gM5Y" frameborder="0" allow="autoplay; fullscreen; encrypted-media; picture-in-picture" allowfullscreen></iframe></div></div></div></details>

## Tools: Extending the Agent

Tools are functions you give an agent so it can act: look things up, run queries, call APIs, write files. You define what they do; the agent decides when to call them and what arguments to pass.

Every deep agent ships with a built-in set of tools, loaded automatically by the SDK. You already saw these appear in the LangSmith trace in the previous lesson, as `Filesystem Tools` on the model call:

<table>
<thead>
<tr><th>Tool(s)</th><th>What it does</th></tr>
</thead>
<tbody>
<tr><td><code>ls</code>, <code>read_file</code>, <code>write_file</code>, <code>edit_file</code>, <code>delete</code></td><td>Read, write, and delete files in the agent's working directory</td></tr>
<tr><td><code>glob</code>, <code>grep</code></td><td>Search files by name pattern or content</td></tr>
<tr><td><code>execute</code></td><td>Run shell commands (requires a sandbox backend)</td></tr>
<tr><td><code>task</code></td><td>Delegate work to a sub-agent with an isolated context</td></tr>
</tbody>
</table>

`delete` is irreversible and recursive: deleting a directory removes it and everything inside it in one call. Keep that in mind if you scope the agent to a broad working directory.

These are always present. The `tools` parameter on `create_deep_agent` is **additive**: whatever you pass is merged with the built-in suite, not substituted for it. A `write_todos` tool for managing a running to-do list is also available, but it's opt-in: add `TodoListMiddleware` to `middleware` to turn it on. You'll see it in Module 4.

```python
agent = create_deep_agent(
    model=model,
    tools=[my_tool_a, my_tool_b],   # merged with the built-ins above
)
```

Step through the sequence to see how each part fits together. The model asks for a tool call, and **Tools**, the built-in runner inside the agent, executes the Python function and returns the result.

<div class="sd-wrap" id="dg-tool-loop"></div>

<script>
buildDiagram({
  id: 'dg-tool-loop',
  participants: ['SDK', 'LLM', 'Tools', 'read_sql'],
  cx: [80, 320, 590, 840],
  bw: 130, bh: 42,
  tby: 20, bby: 480,
  vw: 960, vh: 540,
  partFontSize: 16,
  steps: [
    {
      tag: 'Step 1 of 7 · SDK provides tool schemas',
      caption: 'Before every LLM call, the SDK binds the available tools to the model call. The model receives each tool name, description, and JSON schema, so it knows what tools exist and how to call them.'
    },
    {
      tag: 'Step 2 of 7 · LLM outputs tool_calls',
      caption: 'The LLM decides it needs data and outputs an AIMessage with a tool_calls list: the name of the tool and the arguments to pass. The LLM does not run the tool. It produces a structured request; something else executes it.'
    },
    {
      tag: 'Step 3 of 7 · tool_calls trigger Tools',
      caption: 'The agent runtime detects tool_calls in the AIMessage and routes them to Tools, the built-in runner inside the agent. The AIMessage itself is not passed to Tools; the tool_calls within it are what trigger the routing.'
    },
    {
      tag: 'Step 4 of 7 · Tools executes the call',
      caption: 'Tools reads tool_calls, finds read_sql by name, and calls it with the arguments the LLM supplied. If the LLM requested multiple tools with no dependencies between them, Tools can run them in parallel.'
    },
    {
      tag: 'Step 5 of 7 · Tool returns a result',
      caption: 'read_sql runs the query and returns a string. Tools wraps it in a ToolMessage, linking the result back to the original request via a tool_call_id.'
    },
    {
      tag: 'Step 6 of 7 · ToolMessage feeds back to LLM',
      caption: 'The ToolMessage is appended to the conversation and the LLM is called again with the full updated history. The LLM now sees its original question, its tool_calls, and the results.'
    },
    {
      tag: 'Step 7 of 7 · LLM answers or loops',
      caption: 'With the tool result in context, the LLM generates its next output: a plain AIMessage with a text answer if it has enough information, or another tool_call to keep querying. This is the loop. Open the run in LangSmith to see the full sequence.'
    }
  ],
  buildSteps: function(a) {
    var L = [80, 320, 590, 840], hw = 65, fs = 14;
    return [
      solidArrow(L[0]+hw, L[1]-hw, 82, 'tool schemas', (L[0]+L[1])/2, a, fs),
      labelBox(L[1], 112, 190, ['AIMessage', '{ tool_calls: [...] }'], fs),
      solidArrow(L[1]+hw, L[2]-hw, 205, 'tool_calls', (L[1]+L[2])/2, a, fs),
      solidArrow(L[2]+hw, L[3]-hw, 262, 'read_sql(query=…)', (L[2]+L[3])/2, a, fs),
      dashedArrow(L[3]-hw, L[2]+hw, 322, 'result string', (L[2]+L[3])/2, a, fs),
      solidArrow(L[2]-hw, L[1]+hw, 382, 'ToolMessage', (L[1]+L[2])/2, a, fs),
      labelBox(L[1], 412, 190, ['AIMessage', '(answer or loop)'], fs)
    ];
  }
});
</script>

<script>
(function () {
  var stage = document.getElementById('dg-tool-loop-stage');
  if (!stage) return;
  var svg = stage.querySelector('svg');
  if (!svg) return;
  var ns = 'http://www.w3.org/2000/svg';
  var g = document.createElementNS(ns, 'g');
  g.id = 'dg-tool-loop-hint';
  g.setAttribute('style', 'pointer-events:none');
  var r = document.createElementNS(ns, 'rect');
  r.setAttribute('x','0'); r.setAttribute('y','0');
  r.setAttribute('width','960'); r.setAttribute('height','540');
  r.setAttribute('fill','rgba(3,7,16,0.85)');
  g.appendChild(r);
  var t1 = document.createElementNS(ns, 'text');
  t1.setAttribute('x','480'); t1.setAttribute('y','263');
  t1.setAttribute('text-anchor','middle'); t1.setAttribute('fill','#E5F4FF');
  t1.setAttribute('font-size','18'); t1.setAttribute('font-weight','500');
  t1.setAttribute('font-family','IBM Plex Mono, monospace');
  t1.textContent = 'Click to step through the flow';
  g.appendChild(t1);
  var t2 = document.createElementNS(ns, 'text');
  t2.setAttribute('x','480'); t2.setAttribute('y','287');
  t2.setAttribute('text-anchor','middle'); t2.setAttribute('fill','#7FC8FF');
  t2.setAttribute('font-size','13'); t2.setAttribute('font-family','IBM Plex Mono, monospace');
  t2.textContent = 'or use the ← → buttons below';
  g.appendChild(t2);
  svg.appendChild(g);
  function unfade() {
    var hint = document.getElementById('dg-tool-loop-hint');
    if (hint) hint.style.display = 'none';
    stage.removeEventListener('click', unfade);
    var nb = document.getElementById('dg-tool-loop-next');
    if (nb) nb.removeEventListener('click', unfade);
  }
  stage.addEventListener('click', unfade);
  var nb = document.getElementById('dg-tool-loop-next');
  if (nb) nb.addEventListener('click', unfade);
})();
</script>

<details style="margin:4px 0 16px;">
<summary style="cursor:pointer;font-size:0.9em;color:#40668D;">Parallel tool calls</summary>
<div style="padding:8px 16px 12px;">
<p><code>tool_calls</code> is a list because the model can request more than one tool in the same turn. When those calls have no dependency on each other, Tools can run them simultaneously rather than one at a time.</p>
<p><strong>Example:</strong> an agent with <code>search_web</code> and <code>search_docs</code> tools receives a question that needs both. Both can fire in the same turn; the LLM sees both results together in the next call.</p>
<p><strong>Try it in Lab 3:</strong> ask two unrelated questions in one message: <code>"Which genres have the most tracks, and which employee has handled the most invoices?"</code> Then watch the LangSmith trace to see whether the calls overlap.</p>
</div>
</details>

### Defining a tool with `@tool`

<details style="border:2.5px solid #000;border-radius:6px;background:#fff;margin:1rem 0;"><summary style="padding:10px 16px;cursor:pointer;font-weight:500;font-family:'IBM Plex Mono',monospace;font-size:14px;">Lesson Video Walkthrough</summary><div style="padding:12px 16px 16px;"><div class="video-container" style="max-width:750px;"><div class="video-wrapper"><iframe src="https://share.descript.com/embed/oOMDbVt36V9" frameborder="0" allow="autoplay; fullscreen; encrypted-media; picture-in-picture" allowfullscreen></iframe></div></div></div></details>

Tools are part of the LangChain library. The `@tool` decorator from `langchain_core.tools` is the standard way to define one: write a regular Python function and the decorator turns it into something the model can call. LangChain also ships ready-made tools for common tasks (for example, `TavilySearchResults` for web search), so you don't have to write everything from scratch.

```python
from langchain_core.tools import tool

@tool
def get_stock_price(ticker: str) -> str:
    """Return the latest closing price for a stock ticker symbol."""
    ...  # your implementation
```

<Image src="images/m1.5-tool-anatomy.svg" width="800px" alt="Diagram showing the three parts of a @tool definition: the decorator (@tool) registers it as a LangChain tool, the function name and type annotations form the name and schema, and the docstring is the description the model reads to decide when to call it." />

The description is the most important natural-language signal. Write it as a clear, specific sentence: the model uses it alongside the tool name and argument schema to decide when to reach for this tool versus another.

The return value should be a string (or anything JSON-serializable). It comes back to the model as the tool result and is visible in the conversation.

### A note on action tools

Tools can do more than retrieve information. They can also take actions: write to a database, send a message, create a ticket, or delete a file. Those tools need safety controls. In Lesson 8, we'll cover Human-in-the-Loop approval so a person can review sensitive tool calls before they execute.

---

## Recap

- The `tools=` parameter is additive; your tools merge with the built-in suite (`ls`, `read_file`, `write_file`, `edit_file`, `delete`, `glob`, `grep`, `execute`, `task`); nothing is replaced. `write_todos` is a separate, opt-in tool enabled via `TodoListMiddleware`
- The `@tool` decorator turns a Python function into a callable tool; the docstring is the most important natural-language description the model reads to decide when to use it
- The LLM never executes tools directly; it outputs an `AIMessage` with a `tool_calls` list; Tools reads that and runs the function
- Tools can retrieve information or take actions; Lesson 8 covers how to add Human-in-the-Loop approval for sensitive action tools

---

## References

**Documentation:**

- [Deep Agents overview](https://docs.langchain.com/oss/python/deepagents/overview)
- [Customization (Deep Agents)](https://docs.langchain.com/oss/python/deepagents/customization)
- [deepagents README (GitHub)](https://github.com/langchain-ai/deepagents)
- [text-to-sql-agent example (GitHub)](https://github.com/langchain-ai/deepagents/tree/main/examples/text-to-sql-agent)

</div>

<div data-lang="typescript" markdown="1">

<details style="border:2.5px solid #000;border-radius:6px;background:#fff;margin:1rem 0;"><summary style="padding:10px 16px;cursor:pointer;font-weight:500;font-family:'IBM Plex Mono',monospace;font-size:14px;">Lesson Video Walkthrough</summary><div style="padding:12px 16px 16px;"><div class="video-container" style="max-width:750px;"><div class="video-wrapper"><iframe src="https://share.descript.com/embed/E2a0vp8gM5Y" frameborder="0" allow="autoplay; fullscreen; encrypted-media; picture-in-picture" allowfullscreen></iframe></div></div></div></details>

## Tools: Extending the Agent

Tools are functions you give an agent so it can act: look things up, run queries, call APIs, write files. You define what they do; the agent decides when to call them and what arguments to pass.

Every deep agent ships with a built-in set of tools, loaded automatically by the SDK. You already saw these appear in the LangSmith trace in the previous lesson, as `Filesystem Tools` on the model call:

<table>
<thead>
<tr><th>Tool(s)</th><th>What it does</th></tr>
</thead>
<tbody>
<tr><td><code>ls</code>, <code>read_file</code>, <code>write_file</code>, <code>edit_file</code></td><td>Read and write files in the agent's working directory</td></tr>
<tr><td><code>glob</code>, <code>grep</code></td><td>Search files by name pattern or content</td></tr>
<tr><td><code>execute</code></td><td>Run shell commands (requires a sandbox backend)</td></tr>
<tr><td><code>task</code></td><td>Delegate work to a sub-agent with an isolated context</td></tr>
</tbody>
</table>

These are always present. The `tools` parameter on `createDeepAgent` is **additive**: whatever you pass is merged with the built-in suite, not substituted for it. A `write_todos` tool for managing a running to-do list is also available, but it's opt-in: add `todoListMiddleware` to `middleware` to turn it on. You'll see it in Module 4. The Python SDK also ships a `delete` tool for removing files and directories; TypeScript doesn't have it yet.

```typescript
const agent = createDeepAgent({
  model,
  tools: [myToolA, myToolB],   // merged with the built-ins above
});
```

Step through the sequence to see how each part fits together. The model asks for a tool call, and **Tools**, the built-in runner inside the agent, executes the function and returns the result.

<div class="sd-wrap" id="dg-tool-loop-ts"></div>

<script>
buildDiagram({
  id: 'dg-tool-loop-ts',
  participants: ['SDK', 'LLM', 'Tools', 'readSql'],
  cx: [80, 320, 590, 840],
  bw: 130, bh: 42,
  tby: 20, bby: 480,
  vw: 960, vh: 540,
  partFontSize: 16,
  steps: [
    {
      tag: 'Step 1 of 7 · SDK provides tool schemas',
      caption: 'Before every LLM call, the SDK binds the available tools to the model call. The model receives each tool name, description, and JSON schema, so it knows what tools exist and how to call them.'
    },
    {
      tag: 'Step 2 of 7 · LLM outputs tool_calls',
      caption: 'The LLM decides it needs data and outputs an AIMessage with a tool_calls list: the name of the tool and the arguments to pass. The LLM does not run the tool. It produces a structured request; something else executes it.'
    },
    {
      tag: 'Step 3 of 7 · tool_calls trigger Tools',
      caption: 'The agent runtime detects tool_calls in the AIMessage and routes them to Tools, the built-in runner inside the agent. The AIMessage itself is not passed to Tools; the tool_calls within it are what trigger the routing.'
    },
    {
      tag: 'Step 4 of 7 · Tools executes the call',
      caption: 'Tools reads tool_calls, finds readSql by name, and calls it with the arguments the LLM supplied. If the LLM requested multiple tools with no dependencies between them, Tools can run them in parallel.'
    },
    {
      tag: 'Step 5 of 7 · Tool returns a result',
      caption: 'readSql runs the query and returns a string. Tools wraps it in a ToolMessage, linking the result back to the original request via a tool_call_id.'
    },
    {
      tag: 'Step 6 of 7 · ToolMessage feeds back to LLM',
      caption: 'The ToolMessage is appended to the conversation and the LLM is called again with the full updated history. The LLM now sees its original question, its tool_calls, and the results.'
    },
    {
      tag: 'Step 7 of 7 · LLM answers or loops',
      caption: 'With the tool result in context, the LLM generates its next output: a plain AIMessage with a text answer if it has enough information, or another tool_call to keep querying. This is the loop. Open the run in LangSmith to see the full sequence.'
    }
  ],
  buildSteps: function(a) {
    var L = [80, 320, 590, 840], hw = 65, fs = 14;
    return [
      solidArrow(L[0]+hw, L[1]-hw, 82, 'tool schemas', (L[0]+L[1])/2, a, fs),
      labelBox(L[1], 112, 190, ['AIMessage', '{ tool_calls: [...] }'], fs),
      solidArrow(L[1]+hw, L[2]-hw, 205, 'tool_calls', (L[1]+L[2])/2, a, fs),
      solidArrow(L[2]+hw, L[3]-hw, 262, 'readSql(query=…)', (L[2]+L[3])/2, a, fs),
      dashedArrow(L[3]-hw, L[2]+hw, 322, 'result string', (L[2]+L[3])/2, a, fs),
      solidArrow(L[2]-hw, L[1]+hw, 382, 'ToolMessage', (L[1]+L[2])/2, a, fs),
      labelBox(L[1], 412, 190, ['AIMessage', '(answer or loop)'], fs)
    ];
  }
});
</script>

<script>
(function () {
  var stage = document.getElementById('dg-tool-loop-ts-stage');
  if (!stage) return;
  var svg = stage.querySelector('svg');
  if (!svg) return;
  var ns = 'http://www.w3.org/2000/svg';
  var g = document.createElementNS(ns, 'g');
  g.id = 'dg-tool-loop-ts-hint';
  g.setAttribute('style', 'pointer-events:none');
  var r = document.createElementNS(ns, 'rect');
  r.setAttribute('x','0'); r.setAttribute('y','0');
  r.setAttribute('width','960'); r.setAttribute('height','540');
  r.setAttribute('fill','rgba(3,7,16,0.85)');
  g.appendChild(r);
  var t1 = document.createElementNS(ns, 'text');
  t1.setAttribute('x','480'); t1.setAttribute('y','263');
  t1.setAttribute('text-anchor','middle'); t1.setAttribute('fill','#E5F4FF');
  t1.setAttribute('font-size','18'); t1.setAttribute('font-weight','500');
  t1.setAttribute('font-family','IBM Plex Mono, monospace');
  t1.textContent = 'Click to step through the flow';
  g.appendChild(t1);
  var t2 = document.createElementNS(ns, 'text');
  t2.setAttribute('x','480'); t2.setAttribute('y','287');
  t2.setAttribute('text-anchor','middle'); t2.setAttribute('fill','#7FC8FF');
  t2.setAttribute('font-size','13'); t2.setAttribute('font-family','IBM Plex Mono, monospace');
  t2.textContent = 'or use the ← → buttons below';
  g.appendChild(t2);
  svg.appendChild(g);
  function unfade() {
    var hint = document.getElementById('dg-tool-loop-ts-hint');
    if (hint) hint.style.display = 'none';
    stage.removeEventListener('click', unfade);
    var nb = document.getElementById('dg-tool-loop-ts-next');
    if (nb) nb.removeEventListener('click', unfade);
  }
  stage.addEventListener('click', unfade);
  var nb = document.getElementById('dg-tool-loop-ts-next');
  if (nb) nb.addEventListener('click', unfade);
})();
</script>

<details style="margin:4px 0 16px;">
<summary style="cursor:pointer;font-size:0.9em;color:#40668D;">Parallel tool calls</summary>
<div style="padding:8px 16px 12px;">
<p><code>tool_calls</code> is a list because the model can request more than one tool in the same turn. When those calls have no dependency on each other, Tools can run them simultaneously rather than one at a time.</p>
<p><strong>Example:</strong> an agent with <code>searchWeb</code> and <code>searchDocs</code> tools receives a question that needs both. Both can fire in the same turn; the LLM sees both results together in the next call.</p>
<p><strong>Try it in Lab 3:</strong> ask two unrelated questions in one message: <code>"Which genres have the most tracks, and which employee has handled the most invoices?"</code> Then watch the LangSmith trace to see whether the calls overlap.</p>
</div>
</details>

### Defining a tool with `tool()`

<details style="border:2.5px solid #000;border-radius:6px;background:#fff;margin:1rem 0;"><summary style="padding:10px 16px;cursor:pointer;font-weight:500;font-family:'IBM Plex Mono',monospace;font-size:14px;">Lesson Video Walkthrough</summary><div style="padding:12px 16px 16px;"><div class="video-container" style="max-width:750px;"><div class="video-wrapper"><iframe src="https://share.descript.com/embed/oOMDbVt36V9" frameborder="0" allow="autoplay; fullscreen; encrypted-media; picture-in-picture" allowfullscreen></iframe></div></div></div></details>

Tools are part of the LangChain library. The `tool()` helper from `@langchain/core/tools` is the standard way to define one: write a regular function and pair it with an explicit `{ name, description, schema }` object. The schema, defined with [Zod](https://zod.dev/), tells the model what arguments the tool takes and their types. LangChain also ships ready-made tools for common tasks (for example, `TavilySearch` for web search), so you don't have to write everything from scratch.

```typescript
import { tool } from "langchain";
import { z } from "zod";

const getStockPrice = tool(
  ({ ticker }) => {
    ...  // your implementation
  },
  {
    name: "get_stock_price",
    description: "Return the latest closing price for a stock ticker symbol.",
    schema: z.object({ ticker: z.string() }),
  }
);
```

<Image src="images/m1.5-tool-anatomy-typescript.svg" width="800px" alt="Diagram showing the three parts of a tool() definition: the tool() call wraps the function and registers it as a LangChain tool, the name and schema fields identify it and describe its arguments, and the description is what the model reads to decide when to call it." />

The description is the most important natural-language signal. Write it as a clear, specific sentence: the model uses it alongside the tool name and argument schema to decide when to reach for this tool versus another.

The return value should be a string (or anything JSON-serializable). It comes back to the model as the tool result and is visible in the conversation.

### A note on action tools

Tools can do more than retrieve information. They can also take actions: write to a database, send a message, create a ticket, or delete a file. Those tools need safety controls. In Lesson 8, we'll cover Human-in-the-Loop approval so a person can review sensitive tool calls before they execute.

---

## Recap

- The `tools:` option is additive; your tools merge with the built-in suite (`ls`, `read_file`, `write_file`, `edit_file`, `glob`, `grep`, `execute`, `task`); nothing is replaced. `write_todos` is a separate, opt-in tool enabled via `todoListMiddleware`. Python's built-in suite also includes `delete`, which TypeScript doesn't have yet
- The `tool()` helper turns a plain function into a callable tool; the description is the most important natural-language signal the model reads to decide when to use it
- The LLM never executes tools directly; it outputs an `AIMessage` with a `tool_calls` list; Tools reads that and runs the function
- Tools can retrieve information or take actions; Lesson 8 covers how to add Human-in-the-Loop approval for sensitive action tools

---

## References

**Documentation:**

- [Deep Agents overview](https://docs.langchain.com/oss/javascript/deepagents/overview)
- [Customization (Deep Agents)](https://docs.langchain.com/oss/javascript/deepagents/customization)
- [deepagents README (GitHub)](https://github.com/langchain-ai/deepagents)
- [text-to-sql-agent example (GitHub)](https://github.com/langchain-ai/deepagents/tree/main/examples/text-to-sql-agent)

</div>

</div>

<div class="lt-panel" id="p-quiz" role="tabpanel">

<div data-lang="python">

<h2>Check your understanding</h2>

<MCQ
    question="You pass tools=[my_tool] to create_deep_agent. What happens to the built-in tools like read_file and edit_file?"
    choices='["They are merged with my_tool; both are available", "They are replaced by my_tool", "They are disabled until re-added explicitly", "They move to a separate tool list the agent accesses on request"]'
    correctIndex={0}
    explanation="The tools parameter is additive. Whatever you pass is merged with the SDK's built-in tool suite. Passing your own tools never removes read_file, edit_file, or any other built-in."
/>

<MCQ
    question="When an LLM decides it needs to call a tool, what does it actually produce?"
    choices='["It calls the Python function directly", "A ToolMessage with the result already included", "A plain text description of what it would do", "An AIMessage with a tool_calls list specifying the name and arguments"]'
    correctIndex={3}
    explanation="The LLM never executes code. It outputs an AIMessage containing a tool_calls list with the tool name and arguments. Tools inside the agent reads that list and performs the actual execution."
/>

<MCQ
    question="What information does the model see about a tool before deciding whether to call it?"
    choices='["Only the Python source code", "The tool name, description, and argument schema", "Only the return type", "Only previous successful calls"]'
    correctIndex={1}
    explanation="The model sees the tool name, description, and argument schema. The docstring becomes the main natural-language description, so it should clearly say when the tool should be used."
/>

<MCQ
    question="In the SQL lab, what does the read_sql tool return to the model?"
    choices='["A Python function object", "A ToolMessage containing the database query result", "A new system prompt", "A modified database file"]'
    correctIndex={1}
    explanation="Tools runs read_sql, wraps the returned database result in a ToolMessage, and sends that result back to the model so it can answer."
/>

<MCQ
    question="Why should action tools like database writes or message sending usually have extra safeguards?"
    choices='["Because tools cannot return strings", "Because they may change external state or have irreversible effects", "Because models cannot call them", "Because they replace the built-in tools"]'
    correctIndex={1}
    explanation="Action tools can change external state. This lesson introduces tools; Lesson 8 covers Human-in-the-Loop approval for sensitive actions."
/>

</div>

<div data-lang="typescript">

<h2>Check your understanding</h2>

<MCQ
    question="You pass tools: [myTool] to createDeepAgent. What happens to the built-in tools like read_file and edit_file?"
    choices='["They are merged with myTool; both are available", "They are replaced by myTool", "They are disabled until re-added explicitly", "They move to a separate tool list the agent accesses on request"]'
    correctIndex={0}
    explanation="The tools option is additive. Whatever you pass is merged with the SDK's built-in tool suite. Passing your own tools never removes read_file, edit_file, or any other built-in."
/>

<MCQ
    question="When an LLM decides it needs to invoke a tool, what does it actually produce?"
    choices='["It calls the function directly", "A ToolMessage with the result already included", "A plain text description of what it would do", "An AIMessage with a tool_calls list specifying the name and arguments"]'
    correctIndex={3}
    explanation="The LLM never executes code. It outputs an AIMessage containing a tool_calls list with the tool name and arguments. Tools inside the agent reads that list and performs the actual execution."
/>

<MCQ
    question="What information does the model see about a tool before deciding whether to call it?"
    choices='["Only the source code", "The tool name, description, and argument schema", "Only the return type", "Only previous successful calls"]'
    correctIndex={1}
    explanation="The model sees the tool name, description, and argument schema. The description becomes the main natural-language signal, so it should clearly say when the tool should be used."
/>

<MCQ
    question="In the SQL lab, what does the readSql tool return to the model?"
    choices='["A function object", "A ToolMessage containing the database query result", "A new system prompt", "A modified database file"]'
    correctIndex={1}
    explanation="Tools runs readSql, wraps the returned database result in a ToolMessage, and sends that result back to the model so it can answer."
/>

<MCQ
    question="Why should action tools such as database writes or message sending usually have extra safeguards?"
    choices='["Because tools cannot return strings", "Because they may change external state or have irreversible effects", "Because models cannot call them", "Because they replace the built-in tools"]'
    correctIndex={1}
    explanation="Action tools can change external state. This lesson introduces tools; Lesson 8 covers Human-in-the-Loop approval for sensitive actions."
/>

</div>

</div>

<div class="lt-panel" id="p-lab3" markdown="1" role="tabpanel">

<div data-lang="python" markdown="1">

## A SQL Tool

Open `python/m1/m1.5_scratch_agent_tools.py`, already set up and ready to run.

- One custom tool: `read_sql` for SELECT queries against the Chinook database.
- The agent will decide when to call the tool, generate the SQL query, and use the result to answer in natural language.

The [Chinook](https://github.com/lerocha/chinook-database) database is a standard SQLite music store sample with 11 tables covering artists, albums, tracks, customers, invoices, and employees. The file is already in the repo at `python/m1/chinook.db`.

<Tip>

<details style="border:2.5px solid #000;border-radius:6px;background:#fff;margin:1rem 0;"><summary style="padding:10px 16px;cursor:pointer;font-weight:500;font-family:'IBM Plex Mono',monospace;font-size:14px;">Lab Video Walkthrough</summary><div style="padding:12px 16px 16px;"><div class="video-container" style="max-width:750px;"><div class="video-wrapper"><iframe src="https://share.descript.com/embed/NGWFQ4t65Bs" frameborder="0" allow="autoplay; fullscreen; encrypted-media; picture-in-picture" allowfullscreen></iframe></div></div></div></details>

**[View this run here in LangSmith.](https://smith.langchain.com/public/2b5b2506-a1cf-4a28-99cd-8626c0043451/r/019f1b38-3f1f-7a42-aa30-23e4d39f223a)** Notice the `read_sql` tool call, the SQL query it generated, and the result coming back as a `ToolMessage`.

<details style="border:2.5px solid #000;border-radius:6px;background:#E5F4FF;margin:1rem 0;"><summary style="padding:10px 16px;cursor:pointer;font-weight:500;font-family:'IBM Plex Mono',monospace;font-size:14px;">LangSmith Video Walkthrough</summary><div style="padding:12px 16px 16px;"><div class="video-container" style="max-width:750px;"><div class="video-wrapper"><iframe src="https://share.descript.com/embed/h1InaofLToI" frameborder="0" allow="autoplay; fullscreen; encrypted-media; picture-in-picture" allowfullscreen></iframe></div></div></div></details>

</Tip>

---

### 1. Imports and database connection

Here's how the file sets up its imports and database connection:

```python
import warnings
warnings.filterwarnings("ignore", category=DeprecationWarning)

from pathlib import Path

from deepagents import create_deep_agent
from langchain_community.utilities import SQLDatabase
from langchain_core.tools import tool

from models import model

DB_PATH = Path(__file__).parent / "chinook.db"
db = SQLDatabase.from_uri(f"sqlite:///{DB_PATH}")
```

---

### 2. System prompt

The system prompt tells the agent what the tool is for and asks it to show the SQL it used:

```python
SYSTEM_PROMPT = """You are a SQL analyst with access to the Chinook music store database.

Rules:
- Use read_sql for SELECT queries.
- Do not modify the database.
- If a tool returns an error, revise the SQL and try again.
- Show your SQL in your final answer.
"""
```

---

### 3. Define `read_sql`

`read_sql` is a regular Python function. The `@tool` decorator turns it into a tool the agent can call.

```python
@tool
def read_sql(query: str) -> str:
    """Run a read-only SELECT query against the Chinook music store database."""
    try:
        return str(db.run(query))
    except Exception as e:
        return f"Error: {e}"
```

---

### 4. Wire it up

Pass the tool into `create_deep_agent`. The custom `read_sql` tool is merged with the built-in Deep Agents tools.

```python
agent = create_deep_agent(
    model=model,
    name="SQL_Agent",
    tools=[read_sql],
    system_prompt=SYSTEM_PROMPT,
)

result = agent.invoke(
    {"messages": [{"role": "user", "content": "Which five genres have the most tracks?"}]}
)

print(result["messages"][-1].content)
```

---

### Run it

```bash
cd python
uv run ./m1/m1.5_scratch_agent_tools.py
```

The agent should call `read_sql`, inspect the query result, and answer with the top genres plus the SQL it used.

<div class="callout">
<strong>What is happening</strong><br>
The model does not query SQLite directly. It emits a <code>read_sql</code> tool call with a SQL string as the argument. Tools runs your Python function, wraps the database result in a ToolMessage, and sends that result back to the model. The final answer is generated from that tool result.
</div>

### Take it further: build your own tools

The same `@tool` pattern works for anything Python can do. Describe what you want to your coding agent and let it write the implementation, then drop it into `create_deep_agent(tools=[...])`.

Three ideas to get started:

- **REST API**: fetch live data from any JSON API: weather forecasts, GitHub issues, stock prices. One `@tool` that calls `requests.get(...)` and returns the result is all it takes.
- **Action tool**: tools don't have to be read-only. Post to Slack, create a calendar event, write a row to a spreadsheet. The agent can act, not just retrieve.


</div>

<div data-lang="typescript" markdown="1">

## A SQL Tool

Open `typescript/m1/m1.5_scratch_agent_tools.ts`, already set up and ready to run.

- One custom tool: `readSql` for SELECT queries against the Chinook database.
- The agent will decide when to call the tool, generate the SQL query, and use the result to answer in natural language.

The [Chinook](https://github.com/lerocha/chinook-database) database is a standard SQLite music store sample with 11 tables covering artists, albums, tracks, customers, invoices, and employees. The file is already in the repo at `typescript/m1/chinook.db`.

<Tip>

<details style="border:2.5px solid #000;border-radius:6px;background:#fff;margin:1rem 0;"><summary style="padding:10px 16px;cursor:pointer;font-weight:500;font-family:'IBM Plex Mono',monospace;font-size:14px;">Lab Video Walkthrough</summary><div style="padding:12px 16px 16px;"><div class="video-container" style="max-width:750px;"><div class="video-wrapper"><iframe src="https://share.descript.com/embed/NGWFQ4t65Bs" frameborder="0" allow="autoplay; fullscreen; encrypted-media; picture-in-picture" allowfullscreen></iframe></div></div></div></details>

**[View this run here in LangSmith.](https://smith.langchain.com/public/2b5b2506-a1cf-4a28-99cd-8626c0043451/r/019f1b38-3f1f-7a42-aa30-23e4d39f223a)** Notice the `readSql` tool call, the SQL query it generated, and the result coming back as a `ToolMessage`.

<details style="border:2.5px solid #000;border-radius:6px;background:#E5F4FF;margin:1rem 0;"><summary style="padding:10px 16px;cursor:pointer;font-weight:500;font-family:'IBM Plex Mono',monospace;font-size:14px;">LangSmith Video Walkthrough</summary><div style="padding:12px 16px 16px;"><div class="video-container" style="max-width:750px;"><div class="video-wrapper"><iframe src="https://share.descript.com/embed/h1InaofLToI" frameborder="0" allow="autoplay; fullscreen; encrypted-media; picture-in-picture" allowfullscreen></iframe></div></div></div></details>

</Tip>

---

### 1. Imports and database connection

Here's how the file sets up its imports and database connection:

```typescript
import { DatabaseSync } from "node:sqlite";
import { dirname, join } from "node:path";
import { fileURLToPath } from "node:url";

import { z } from "zod";
import { tool } from "langchain";
import { createDeepAgent } from "deepagents";

import { model } from "../models.js";

const __dirname = dirname(fileURLToPath(import.meta.url));
const DB_PATH = join(__dirname, "chinook.db");
const db = new DatabaseSync(DB_PATH);
```

---

### 2. System prompt

The system prompt tells the agent what the tool is for and asks it to show the SQL it used:

```typescript
const SYSTEM_PROMPT = `You are a SQL analyst with access to the Chinook music store database.

Rules:
- Use read_sql for SELECT queries.
- Do not modify the database.
- If a tool returns an error, revise the SQL and try again.
- Show your SQL in your final answer.
`;
```

---

### 3. Define `readSql`

`readSql` is defined with the `tool()` helper from `@langchain/core/tools`, which pairs a plain function with a name, description, and Zod schema.

```typescript
const readSql = tool(
  ({ query }) => {
    try {
      const rows = db.prepare(query).all();
      return JSON.stringify(rows);
    } catch (e) {
      return `Error: ${e}`;
    }
  },
  {
    name: "read_sql",
    description: "Run a read-only SELECT query against the Chinook music store database.",
    schema: z.object({ query: z.string() }),
  }
);
```

---

### 4. Wire it up

Pass the tool into `createDeepAgent`. The custom `readSql` tool is merged with the built-in Deep Agents tools.

```typescript
const agent = createDeepAgent({
  model,
  name: "SQL_Agent",
  tools: [readSql],
  systemPrompt: SYSTEM_PROMPT,
});

const result = await agent.invoke({
  messages: [{ role: "user", content: "Which five genres have the most tracks?" }],
});

console.log(result.messages.at(-1)?.content);
```

---

### Run it

```bash
cd typescript
pnpm tsx ./m1/m1.5_scratch_agent_tools.ts
```

The agent should call `readSql`, inspect the query result, and answer with the top genres plus the SQL it used.

<div class="callout">
<strong>What is happening</strong><br>
The model does not query SQLite directly. It emits a <code>readSql</code> tool call with a SQL string as the argument. Tools runs your function, wraps the database result in a ToolMessage, and sends that result back to the model. The final answer is generated from that tool result.
</div>

### Take it further: build your own tools

The same `tool()` pattern works for anything Node.js can do. Describe what you want to your coding agent and let it write the implementation, then drop it into `createDeepAgent({ tools: [...] })`.

Three ideas to get started:

- **REST API**: fetch live data from any JSON API: weather forecasts, GitHub issues, stock prices. One `tool()` that calls `fetch(...)` and returns the result is all it takes.
- **Action tool**: tools don't have to be read-only. Post to Slack, create a calendar event, write a row to a spreadsheet. The agent can act, not just retrieve.


</div>

</div>

<div class="lt-panel" id="p-homework" markdown="1" role="tabpanel">

<div data-lang="python" markdown="1">

## Build Your Own Custom Tool

The lab wired up one custom tool for one fixed topic (SQL over the Chinook database). This homework asks you to do the same thing, but for a topic of your own choosing. There's no single correct answer here: two people doing this homework could end up with two completely different tools and agents.

Open `python/m1/m1.5_homework.py`.

1. **TODO 1**: write your own `@tool`-decorated function. Pick any topic (a game, a show, your favorite band's discography, local trivia, etc.) and give the tool a small lookup of facts to draw from. A plain Python dict is enough, no external API or key required.
2. **TODO 2**: write a system prompt that gives your agent a persona of your choosing and tells it to call your tool before answering.

### Run it

```bash
cd python
uv run ./m1/m1.5_homework.py
```

Stuck, or want to see the finished shape before you start? `m1.5_homework_filled.py` is a reference copy with every TODO filled in (using retro video game trivia and a hype sports-announcer persona); run it the same way to see what "done" looks like end to end.

Try to avoid peeking unless you're really stuck, you'll get more out of this by working through the TODOs yourself first.

</div>

<div data-lang="typescript" markdown="1">

## Build Your Own Custom Tool

The lab wired up one custom tool for one fixed topic (SQL over the Chinook database). This homework asks you to do the same thing, but for a topic of your own choosing. There's no single correct answer here: two people doing this homework could end up with two completely different tools and agents.

Open `typescript/m1/m1.5_homework.ts`.

1. **TODO 1**: write your own tool using the `tool()` helper. Pick any topic (a game, a show, your favorite band's discography, local trivia, etc.) and give the tool a small lookup of facts to draw from. A plain object is enough, no external API or key required.
2. **TODO 2**: write a system prompt that gives your agent a persona of your choosing and tells it to call your tool before answering.

### Run it

```bash
cd typescript
pnpm tsx ./m1/m1.5_homework.ts
```

Stuck, or want to see the finished shape before you start? `m1.5_homework_filled.ts` is a reference copy with every TODO filled in (using retro video game trivia and a hype sports-announcer persona); run it the same way to see what "done" looks like end to end.

Try to avoid peeking unless you're really stuck, you'll get more out of this by working through the TODOs yourself first.

</div>

</div>

<script>
(function () {
  var tabs = document.querySelectorAll('.lt-tab');
  function show(p) {
    tabs.forEach(function (t) {
      var on = t.getAttribute('data-p') === p;
      t.classList.toggle('active', on);
      t.setAttribute('aria-selected', on ? 'true' : 'false');
    });
    document.querySelectorAll('.lt-panel').forEach(function (panel) {
      panel.classList.toggle('active', panel.id === 'p-' + p);
    });
  }
  tabs.forEach(function (t) {
    t.addEventListener('click', function () { show(t.getAttribute('data-p')); });
  });
})();
</script>
