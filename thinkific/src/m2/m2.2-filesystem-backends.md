[For translation, open lesson in new tab and use Chrome translate](https://langchain-ai.github.io/lca-lessons/deepagents/m2/m2.2-filesystem-backends.html)

<style>@import url('../shared/sd-components.css');</style>
<script src="../shared/sd-components.js"></script>
<script src="https://cdn.jsdelivr.net/npm/mermaid@11/dist/mermaid.min.js"></script>
<script>mermaid.initialize({ startOnLoad: true, theme: 'base', themeVariables: { background: '#F2FAFF', primaryColor: '#E5F4FF', primaryBorderColor: '#006DDD', primaryTextColor: '#030710', lineColor: '#2F4B68' } });</script>

<div class="page-lang-switch" data-page-lang-switch>
  <button class="page-lang-btn" data-page-lang="python" aria-selected="false">Python</button>
  <button class="page-lang-btn" data-page-lang="typescript" aria-selected="false">TypeScript</button>
</div>

# Filesystem Backends

<style>
.lt-bar {
  display: flex;
  flex-wrap: wrap;
  gap: 20px;
  margin: 28px 0 0;
  border-bottom: 2px solid #CCE9FF;
}
.lt-group { display: flex; gap: 3px; }
.lt-fs   { --c: #006DDD; }
.lt-quiz { --c: #7C3AED; }
.lt-wrap { --c: #B45309; }
.lt-homework { --c: #0E9F6E; }
.lt-tab {
  font: 500 14px 'IBM Plex Mono', monospace;
  padding: 9px 14px;
  border: none;
  background: transparent;
  color: #40668D;
  cursor: pointer;
  border-bottom: 3px solid transparent;
  margin-bottom: -2px;
  border-radius: 6px 6px 0 0;
  transition: background .15s, color .15s, border-color .15s;
  white-space: nowrap;
}
.lt-tab:hover { background: #F2FAFF; color: #030710; }
.lt-tab.active {
  color: var(--c);
  border-bottom-color: var(--c);
  background: #fff;
}
.lt-panel { display: none; padding-top: 24px; }
.lt-panel.active { display: block; }
@media (max-width: 600px) {
  .lt-bar { flex-wrap: nowrap; overflow-x: auto; gap: 12px; }
  .lt-tab { padding: 8px 10px; font-size: 13px; }
}
</style>

<div class="lt-bar" role="tablist" aria-label="Lesson sections">
  <div class="lt-group lt-fs">
    <button class="lt-tab active" data-p="fs" role="tab" aria-selected="true">Filesystem Backends</button>
  </div>
  <div class="lt-group lt-wrap">
    <button class="lt-tab" data-p="lab1" role="tab" aria-selected="false">Lab</button>
  </div>
  <div class="lt-group lt-quiz">
    <button class="lt-tab" data-p="quiz" role="tab" aria-selected="false">Quiz</button>
  </div>
  <div class="lt-group lt-homework">
    <button class="lt-tab" data-p="homework" role="tab" aria-selected="false">Homework</button>
  </div>
</div>

<div class="lt-panel active" id="p-fs" markdown="1" role="tabpanel">

<details style="border:2.5px solid #000;border-radius:6px;background:#fff;margin:1rem 0;"><summary style="padding:10px 16px;cursor:pointer;font-weight:500;font-family:'IBM Plex Mono',monospace;font-size:14px;">Lesson Video Walkthrough</summary><div style="padding:12px 16px 16px;"><div class="video-container" style="max-width:750px;"><div class="video-wrapper"><iframe src="https://share.descript.com/embed/i6oUY8Y8h2H" frameborder="0" allow="autoplay; fullscreen; encrypted-media; picture-in-picture" allowfullscreen></iframe></div></div></div></details>

The agent always sees the same filesystem tools: `ls`, `read_file`, `write_file`, `edit_file`, `glob`, and `grep`. A filesystem backend decides where those files are actually stored.

This lesson covers the built-in filesystem backends and how to choose between them.

<div class="mermaid">
graph LR
    A[Agent filesystem tools] --> B[Backend]
    B --> S[StateBackend\ncurrent thread state]
    B --> F[FilesystemBackend\nlocal disk]
    B --> C[Custom backend\nS3, GitHub, other storage]

    classDef agent fill:#CCE9FF,stroke:#006DDD,stroke-width:2px,color:#030710
    classDef backend fill:#7FC8FF,stroke:#006DDD,stroke-width:2px,color:#030710
    classDef store fill:#E5F4FF,stroke:#40668D,stroke-width:1px,color:#030710

    class A agent
    class B backend
    class S,F,C store
</div>

<p><em>All filesystem backends expose the same tools. The backend adapts each call to a specific storage implementation.</em></p>

---

## The filesystem backends

You can select different backends depending on where you want files to live. This lesson focuses on the built-in options that do not require a persistent store.

**StateBackend**: the default. Files live in the agent's saved state for the current thread. This is fast and zero-config, and it is good for scratch work.

**FilesystemBackend / local disk**: reads and writes files on the local disk, scoped to a `root_dir` you specify. Use it when the agent should work with real files in a local directory.

**CompositeBackend**: routes different path prefixes to different backends. It lets one agent use StateBackend for normal scratch files while routing a specific directory, such as `/reference/`, to local disk.

**Custom backend**: implement the backend interface yourself to plug in another storage system, such as S3, GitHub, or a proprietary store.

<div class="callout">
<strong>A naming note: "the agent's filesystem" vs. <code>FilesystemBackend</code></strong>
<br><br>
"Filesystem" is used two ways here:
<ul style="margin-top:8px;">
<li>The agent's filesystem is the general capability: the <code>ls</code> / <code>read_file</code> / <code>write_file</code> / <code>edit_file</code> / <code>glob</code> / <code>grep</code> tools every Deep Agent has, no matter which backend is behind them.</li>
<li><code>FilesystemBackend</code> is just one of the four backends above: the one that stores those files on local disk. <code>StateBackend</code>, <code>CompositeBackend</code>, and a custom backend implement the exact same tools; they just store the data somewhere else.</li>
</ul>
Picking "a filesystem backend" doesn't mean picking <code>FilesystemBackend</code> specifically.
</div>

---

<details style="border:2.5px solid #000;border-radius:6px;background:#fff;margin:1rem 0;"><summary style="padding:10px 16px;cursor:pointer;font-weight:500;font-family:'IBM Plex Mono',monospace;font-size:14px;">Lesson Video Walkthrough</summary><div style="padding:12px 16px 16px;"><div class="video-container" style="max-width:750px;"><div class="video-wrapper"><iframe src="https://share.descript.com/embed/Fzcxn1OpG7D" frameborder="0" allow="autoplay; fullscreen; encrypted-media; picture-in-picture" allowfullscreen></iframe></div></div></div></details>

<div style="display:flex;gap:0;align-items:baseline;margin:1.5rem 0 0.25rem;">
  <h2 style="flex:1;text-align:center;padding-right:24px;margin:0;">Single Backend</h2>
  <span style="flex-shrink:0;font-size:1.4em;font-weight:700;color:#030710;">vs</span>
  <h2 style="flex:1;text-align:center;padding-left:24px;margin:0;">Composite Backend</h2>
</div>

<div style="display:flex;gap:0;align-items:flex-start;margin:0.5rem 0 1.25rem;">
<div style="flex:1;padding-right:24px;display:flex;flex-direction:column;align-items:center;">
<p style="font-weight:600;font-size:13px;color:#40668D;margin:0 0 8px;">Single backend</p>
<div class="mermaid">
graph TB
    A1[Agent] --> SB[StateBackend]
    SB --> D1[Thread state]

    classDef agent fill:#CCE9FF,stroke:#006DDD,stroke-width:2px,color:#030710
    classDef backend fill:#7FC8FF,stroke:#006DDD,stroke-width:2px,color:#030710
    classDef store fill:#E5F4FF,stroke:#40668D,stroke-width:1px,color:#030710

    class A1 agent
    class SB backend
    class D1 store
</div>
</div>
<div style="width:1px;background:#CCE9FF;align-self:stretch;flex-shrink:0;"></div>
<div style="flex:1;padding-left:24px;display:flex;flex-direction:column;align-items:center;">
<p style="font-weight:600;font-size:13px;color:#40668D;margin:0 0 8px;">Composite backend</p>
<div class="mermaid">
graph TB
    A2[Agent] --> CB[CompositeBackend]
    CB -->|"/reference/"| D2[Local disk]
    CB -->|"everything else"| ST[Thread state]

    classDef agent fill:#CCE9FF,stroke:#006DDD,stroke-width:2px,color:#030710
    classDef backend fill:#7FC8FF,stroke:#006DDD,stroke-width:2px,color:#030710
    classDef disk fill:#E5F4FF,stroke:#006DDD,stroke-width:1px,color:#030710
    classDef state fill:#E5F4FF,stroke:#40668D,stroke-width:1px,color:#030710

    class A2 agent
    class CB backend
    class D2 disk
    class ST state
</div>
</div>
</div>

## Single backend

<div data-lang="python" markdown="1">

Pass any backend directly to `create_deep_agent`. If you do not pass one, Deep Agents uses `StateBackend()` by default.

```python
from deepagents import create_deep_agent
from deepagents.backends import StateBackend

agent = create_deep_agent(
    model=model,
    backend=StateBackend(),
)
```

Use `FilesystemBackend` when you want the agent's filesystem tools to read and write real local files under a root directory:

```python
from deepagents import create_deep_agent
from deepagents.backends import FilesystemBackend

agent = create_deep_agent(
    model=model,
    backend=FilesystemBackend(root_dir="/path/to/project", virtual_mode=True),
)
```

</div>

<div data-lang="typescript" markdown="1">

Pass any backend directly to `createDeepAgent`. If you do not pass one, Deep Agents uses `new StateBackend()` by default.

```typescript
import { createDeepAgent, StateBackend } from "deepagents";

const agent = createDeepAgent({
  model,
  backend: new StateBackend(),
});
```

Use `FilesystemBackend` when you want the agent's filesystem tools to read and write real local files under a root directory:

```typescript
import { createDeepAgent, FilesystemBackend } from "deepagents";

const agent = createDeepAgent({
  model,
  backend: new FilesystemBackend({ rootDir: "/path/to/project", virtualMode: true }),
});
```

</div>

---

## Composite backend

Sometimes one backend is not enough. You may want ordinary scratch files to stay in thread state, but a specific directory to map to real local files.

`CompositeBackend` resolves this by routing path prefixes to different backends. Everything not matched by a route falls through to the default backend.

<div data-lang="python" markdown="1">

Pass the default backend with the `default` keyword and per-prefix backends with `routes`:

```python
from pathlib import Path

from deepagents import create_deep_agent
from deepagents.backends import CompositeBackend, FilesystemBackend, StateBackend

reference_dir = Path(__file__).parent / "reference"

agent = create_deep_agent(
    model=model,
    backend=CompositeBackend(
        default=StateBackend(),
        routes={
            "/reference/": FilesystemBackend(
                root_dir=str(reference_dir),
                virtual_mode=True,
            ),
        },
    ),
)
```

</div>

<div data-lang="typescript" markdown="1">

`CompositeBackend` takes the default backend and the routes as positional arguments:

```typescript
import { dirname, join } from "node:path";
import { fileURLToPath } from "node:url";

import {
  CompositeBackend,
  FilesystemBackend,
  StateBackend,
  createDeepAgent,
} from "deepagents";

const __dirname = dirname(fileURLToPath(import.meta.url));
const referenceDir = join(__dirname, "reference");

const agent = createDeepAgent({
  model,
  backend: new CompositeBackend(new StateBackend(), {
    "/reference/": new FilesystemBackend({
      rootDir: referenceDir,
      virtualMode: true,
    }),
  }),
});
```

</div>

- `/reference/notes.md` → local file at `reference/notes.md`
- Everything else → `StateBackend`, scoped to the current thread's saved state

---

## Permissions

<div data-lang="python" markdown="1">
Permissions are enforced in code, not in the prompt. The model cannot bypass them. Pass a list of `FilesystemPermission` rules to `create_deep_agent`. Rules are evaluated in order and the first match wins. If nothing matches, the operation is allowed.

Each rule has three fields:

| Field | Values | Default | Description |
|---|---|---|---|
| `operations` | `"read"`, `"write"` | - | `"read"` covers `ls`, `read_file`, `glob`, `grep`. `"write"` covers `write_file`, `edit_file`, `delete`. |
| `paths` | glob patterns | - | e.g. `["/reference/**"]`. Supports `**` and `{a,b}` alternation. |
| `mode` | `"allow"`, `"deny"`, `"interrupt"` | `"allow"` | `"interrupt"` pauses for human approval instead of blocking. |

```python
from pathlib import Path

from deepagents import FilesystemPermission, create_deep_agent
from deepagents.backends import CompositeBackend, FilesystemBackend, StateBackend

reference_dir = Path(__file__).parent / "reference"

agent = create_deep_agent(
    model=model,
    backend=CompositeBackend(
        default=StateBackend(),
        routes={
            "/reference/": FilesystemBackend(
                root_dir=str(reference_dir),
                virtual_mode=True,
            ),
        },
    ),
    permissions=[
        FilesystemPermission(
            operations=["write"],
            paths=["/reference/**"],
            mode="deny",
        ),
    ],
)
```

</div>

<div data-lang="typescript" markdown="1">

Permissions are enforced in code, not in the prompt. The model cannot bypass them. Pass a `permissions` array of `FilesystemPermission` rules to `createDeepAgent`. Rules are evaluated in order and the first match wins. If nothing matches, the operation is allowed.

Each rule has three fields:

| Field | Values | Default | Description |
|---|---|---|---|
| `operations` | `"read"`, `"write"` | - | `"read"` covers `ls`, `read_file`, `glob`, `grep`. `"write"` covers `write_file`, `edit_file`. |
| `paths` | glob patterns | - | e.g. `["/reference/**"]`. Supports `**` and `{a,b}` alternation. |
| `mode` | `"allow"`, `"deny"` | `"allow"` | TypeScript does not yet support Python's `"interrupt"` mode for pausing on human approval. |

```typescript
import { dirname, join } from "node:path";
import { fileURLToPath } from "node:url";

import {
  CompositeBackend,
  FilesystemBackend,
  StateBackend,
  createDeepAgent,
  type FilesystemPermission,
} from "deepagents";

const __dirname = dirname(fileURLToPath(import.meta.url));
const referenceDir = join(__dirname, "reference");

const agent = createDeepAgent({
  model,
  backend: new CompositeBackend(new StateBackend(), {
    "/reference/": new FilesystemBackend({
      rootDir: referenceDir,
      virtualMode: true,
    }),
  }),
  permissions: [
    {
      operations: ["write"],
      paths: ["/reference/**"],
      mode: "deny",
    } satisfies FilesystemPermission,
  ],
});
```

</div>

The agent can read `/reference/`, but it cannot write to it. Other paths still use the default `StateBackend` route.

---

## Restricting the tool list itself

Permission rules control where an operation can happen. The `tools` option on the filesystem middleware controls which tools the model can see: pass it an allowlist of tool names, and anything left off is hidden. `read_file` must always be included.

<div data-lang="python" markdown="1">

```python
from deepagents import create_deep_agent, FilesystemMiddleware

agent = create_deep_agent(
    model=model,
    middleware=[FilesystemMiddleware(tools=["read_file", "grep", "ls"])],
)
```

The allowlist accepts `ls`, `read_file`, `write_file`, `edit_file`, `delete`, `glob`, `grep`, and `execute`.

</div>

<div data-lang="typescript" markdown="1">

```typescript
import { createDeepAgent, createFilesystemMiddleware } from "deepagents";

const agent = createDeepAgent({
  model,
  middleware: [createFilesystemMiddleware({ tools: ["read_file", "grep", "ls"] })],
});
```

The allowlist accepts `ls`, `read_file`, `write_file`, `edit_file`, `glob`, `grep`, and `execute`.

</div>

---

## Recap

- All filesystem backends expose the same tool surface
- **StateBackend** is the default and is good for scratch files in the current thread
- **FilesystemBackend / local disk** maps filesystem tools to real local files under a root directory
- **CompositeBackend** lets you route different path prefixes to different backends
- Permissions are enforced in code, not in the prompt

## Next

Complete the **Lab** and **Quiz** above, then continue to the next lesson on execution backends that add a shell `execute` tool on top of the filesystem surface.

---

## References

<div data-lang="python" markdown="1">

**Documentation:**

- [Filesystem Backends](https://docs.langchain.com/oss/python/deepagents/backends)
- [Permissions](https://docs.langchain.com/oss/python/deepagents/permissions)

</div>

<div data-lang="typescript" markdown="1">

**Documentation:**

- [Filesystem Backends](https://docs.langchain.com/oss/javascript/deepagents/backends)
- [Permissions](https://docs.langchain.com/oss/javascript/deepagents/permissions)

</div>

</div>

<div class="lt-panel" id="p-quiz" role="tabpanel">

<h2>Check your understanding</h2>

<MCQ
    question="What do all filesystem backends have in common?"
    choices='["They all persist files across threads", "They all require local disk access", "They all use the same underlying storage system", "They all expose the same tool surface (ls, read_file, write_file, etc.); the agent cannot tell which backend it is using"]'
    correctIndex={3}
    explanation="All backends implement the same filesystem interface: ls, read_file, write_file, edit_file, glob, grep. The agent code stays the same regardless of which backend you choose."
/>

<MCQ
    question="When would you use FilesystemBackend?"
    choices='["When the agent should read and write real local files under a root directory", "When the agent should never use files", "Only when using memory", "Only when deploying to production"]'
    correctIndex={0}
    explanation="FilesystemBackend maps the agent's filesystem tools to real files under a local root directory."
/>

<MCQ
    question="What does CompositeBackend do?"
    choices='["Routes different path prefixes to different backends", "Converts files into model weights", "Deletes files after every tool call", "Makes all files read-only"]'
    correctIndex={0}
    explanation="CompositeBackend lets one agent route paths like /reference/ to one backend while everything else falls back to another backend."
/>

<MCQ
    question="How are FilesystemPermission rules evaluated?"
    choices='["All matching rules are applied in sequence", "The most specific path pattern wins", "Rules are evaluated in reverse order; the last match wins", "Rules are evaluated in order; the first match wins. If nothing matches, the operation is allowed"]'
    correctIndex={3}
    explanation="FilesystemPermission rules are evaluated in order. The first rule whose operations and paths match wins. If no rule matches, the operation is allowed by default."
/>

</div>

<div class="lt-panel" id="p-lab1" markdown="1" role="tabpanel">

## Lab: Filesystem Permissions

<Tip>

<details style="border:2.5px solid #000;border-radius:6px;background:#fff;margin:1rem 0;"><summary style="padding:10px 16px;cursor:pointer;font-weight:500;font-family:'IBM Plex Mono',monospace;font-size:14px;">Lab Video Walkthrough</summary><div style="padding:12px 16px 16px;"><div class="video-container" style="max-width:750px;"><div class="video-wrapper"><iframe src="https://share.descript.com/embed/q4b8LOnnehl" frameborder="0" allow="autoplay; fullscreen; encrypted-media; picture-in-picture" allowfullscreen></iframe></div></div></div></details>

**[View this run here in LangSmith.](https://smith.langchain.com/public/d8bfb6b0-74f6-4c2c-a5cd-ea192dcf7b39/r/019f1fd7-af20-7ef2-a3a9-7744d88058d3)** Notice the agent reading `/reference/chinook-sales.md` successfully, then getting blocked on the write.

<details style="border:2.5px solid #000;border-radius:6px;background:#E5F4FF;margin:1rem 0;"><summary style="padding:10px 16px;cursor:pointer;font-weight:500;font-family:'IBM Plex Mono',monospace;font-size:14px;">LangSmith Video Walkthrough</summary><div style="padding:12px 16px 16px;"><div class="video-container" style="max-width:750px;"><div class="video-wrapper"><iframe src="https://share.descript.com/embed/CywMBDM59XU" frameborder="0" allow="autoplay; fullscreen; encrypted-media; picture-in-picture" allowfullscreen></iframe></div></div></div></details>

</Tip>

The lab agent uses a `CompositeBackend` with normal scratch files in `StateBackend` and `/reference/` routed to a local `FilesystemBackend`. A `FilesystemPermission` rule denies all writes to `/reference/**`.

The agent is given a reference file, asked to read it, then asked to add a note. You'll see it read successfully and get blocked on the write.

<div data-lang="python" markdown="1">

Before running it, make sure your model API key from setup is available in the Python environment.

```python
# python/m2/m2.2_agent.py
from pathlib import Path

from deepagents import FilesystemPermission, create_deep_agent
from deepagents.backends import CompositeBackend, FilesystemBackend, StateBackend

from models import model

reference_dir = Path(__file__).parent / "reference"
reference_dir.mkdir(exist_ok=True)
(reference_dir / "chinook-sales.md").write_text("""\
# Chinook Sales Reference

You are a sales representative for Chinook Digital Music Store.

Responsibilities:
- Look up customer accounts and purchase history
- Recommend music based on genre and artist preferences
- Answer questions about artists, albums, tracks, and invoices
""")

agent = create_deep_agent(
    model=model,
    backend=CompositeBackend(
        default=StateBackend(),
        routes={
            "/reference/": FilesystemBackend(
                root_dir=str(reference_dir),
                virtual_mode=True,
            ),
        },
    ),
    permissions=[
        FilesystemPermission(
            operations=["write"],
            paths=["/reference/**"],
            mode="deny",
        ),
    ],
)

result = agent.invoke(
    {
        "messages": [
            {
                "role": "user",
                "content": (
                    "Read /reference/chinook-sales.md, then add this note to it: "
                    "'Current promotion: 20% off all Jazz albums through end of month.'"
                ),
            }
        ]
    },
    config={"configurable": {"thread_id": "lab-m2.2"}},
)

print(result["messages"][-1].content)
```

From the repo root:

```bash
cd python
uv run ./m2/m2.2_agent.py
```

Example output will vary by model, but it should show the same behavior:

```
I read /reference/chinook-sales.md successfully, but I could not update it because writes to /reference/** are denied.

Current reference:
# Chinook Sales Reference
...

The note I would add is:
- Current promotion: 20% off all Jazz albums through end of month.
```

**What to look for:**

- The agent reads `/reference/chinook-sales.md` without issue
- When it tries to write, it reports that the file is read-only or write-protected
- Other paths still use the default state-backed filesystem route

</div>

<div data-lang="typescript" markdown="1">

Before running it, make sure your model API key from setup is available in the TypeScript environment.

```typescript
// typescript/m2/m2.2_agent.ts
import { dirname, join } from "node:path";
import { mkdirSync, writeFileSync } from "node:fs";
import { fileURLToPath } from "node:url";

import { context } from "langchain";
import {
  CompositeBackend,
  FilesystemBackend,
  StateBackend,
  createDeepAgent,
  type FilesystemPermission,
} from "deepagents";

import { model } from "../models.js";

const __dirname = dirname(fileURLToPath(import.meta.url));

// Set up a read-only reference directory the agent can consult but not modify.
const referenceDir = join(__dirname, "reference");
mkdirSync(referenceDir, { recursive: true });
writeFileSync(
  join(referenceDir, "chinook-sales.md"),
  `\
# Chinook Sales Reference

You are a sales representative for Chinook Digital Music Store.

Responsibilities:
- Look up customer accounts and purchase history
- Recommend music based on genre and artist preferences
- Answer questions about artists, albums, tracks, and invoices
`
);

const agent = createDeepAgent({
  model,
  backend: new CompositeBackend(new StateBackend(), {
    "/reference/": new FilesystemBackend({
      rootDir: referenceDir,
      virtualMode: true,
    }),
  }),
  permissions: [
    {
      operations: ["write"],
      paths: ["/reference/**"],
      mode: "deny",
    } satisfies FilesystemPermission,
  ],
});

// Note: in Python deepagents, permission errors are returned to the LLM as tool
// messages so it can respond gracefully. In TypeScript deepagents, they surface
// as a thrown MiddlewareError (a langchainjs ReactAgent bug — ToolNode is not
// constructed with handleToolErrors:true). Wrapping here until that is fixed.
try {
  const result = await agent.invoke(
    {
      messages: [
        {
          role: "user",
          content: context`
            Read /reference/chinook-sales.md, then add this note to it:
            'Current promotion: 20% off all Jazz albums through end of month.'`,
        },
      ],
    },
    { configurable: { thread_id: "lab-m2.2" } }
  );
  console.log(result.messages.at(-1)?.content);
} catch (e) {
  const err = e instanceof Error ? e : new Error(String(e));
  const cause = err.cause instanceof Error ? err.cause : undefined;
  console.log("Permission enforced:", cause?.message ?? err.message);
}
```

From the repo root:

```bash
cd typescript
pnpm tsx ./m2/m2.2_agent.ts
```

Example output will vary by model, but it should show the same behavior:

```
Permission enforced: Error: permission denied for write on /reference/chinook-sales.md
```

**What to look for:**

- The agent reads `/reference/chinook-sales.md` without issue
- The write attempt throws instead of returning gracefully to the model — the `catch` block reports the denial, rather than the agent narrating it
- Other paths still use the default state-backed filesystem route

</div>

</div>

<div class="lt-panel" id="p-homework" markdown="1" role="tabpanel">

<div data-lang="python" markdown="1">

## Configure Your Own Filesystem Backend

The lab wired up one fixed setup: a `CompositeBackend` routing a single reference file to local disk, with a `FilesystemPermission` rule denying all writes to it. This homework asks you to do the same thing, but for a file-based task of your own choosing.

Open `python/m2/m2.2_homework.py`.

Your choice here changes what actually happens when you run your file, compared to the lab:

| Backend | What happens to your file | Compared to the lab |
|---|---|---|
| `StateBackend` | Lives only in the thread's saved state; disappears once the thread ends, nothing touches disk | The lab wrote a real file to disk; this skips that entirely |
| `FilesystemBackend` (`virtual_mode=True`) | A real file on disk under whatever `root_dir` you set; still there after the script exits | Same real-disk behavior as the lab's `/reference/` route, but applied to your whole filesystem instead of one path |
| `CompositeBackend` | A real file on disk under `root_dir`, while everything else stays in thread state | Closest match to the lab's exact setup: one real path on disk, everything else in state |

Whichever you pick, `FilesystemPermission` rules behave the same: they're checked against the path before the backend is touched, so the same allow/deny/interrupt outcome happens no matter where the file actually lives.

1. **TODO 1**: pick a topic for a small text file (a packing list, a journal, a recipe box, meeting notes, etc.), seed it with some starting content, and configure any backend you like: `StateBackend`, `FilesystemBackend`, or a `CompositeBackend` routing between them.
2. **TODO 2**: write a task message that has the agent read your file, then write or edit it, and optionally add a `FilesystemPermission` rule that changes what the agent is allowed to do.

### Run it

```bash
cd python
uv run ./m2/m2.2_homework.py
```

Stuck, or want to see the finished shape before you start? `m2.2_homework_filled.py` is a reference copy with every TODO filled in (using a recipe box with one heirloom recipe locked against writes); run it the same way to see what "done" looks like end to end.

Try to avoid peeking unless you're really stuck, you'll get more out of this by working through the TODOs yourself first.

---

<details markdown="1" style="border:2.5px solid #7FC8FF;border-radius:6px;background:#000;margin:1rem 0;"><summary style="padding:15px 16px;display:flex;align-items:center;cursor:pointer;font-weight:500;font-family:'IBM Plex Mono',monospace;font-size:14px;color:#7FC8FF;">Optional Extension: Want To Code From Scratch?</summary><div style="padding:12px 16px 16px;background:#fff;color:#000;border-radius:0 0 4px 4px;" markdown="1">

**What you'll build:** a filesystem tool allowlist that hides `write_file` from the model entirely, instead of letting it call the tool and blocking the call with a permission rule.

**Requirements:**

- Write your own `create_deep_agent(model=model, backend=..., middleware=[FilesystemMiddleware(tools=[...])])` call from scratch, in a new file under `python/m2/`.
- Configure a `CompositeBackend` routing one directory to a `FilesystemBackend(root_dir=..., virtual_mode=True)` (same shape as the lab), and seed it with a starting file.
- Add a `FilesystemMiddleware(tools=[...])` allowlist that leaves `write_file` (and/or `edit_file`) off the list, so the model never even sees that tool exists. `read_file` must stay on the list.
- Write a task that asks the agent to read the file, then also asks it to edit it. Check the trace/messages: no `write_file`/`edit_file` tool call shows up at all, since the model was never given that tool, unlike a `FilesystemPermission` deny rule, where the model calls the tool and only then gets blocked.

</div></details>

</div>

<div data-lang="typescript" markdown="1">

## Configure Your Own Filesystem Backend

The lab wired up one fixed setup: a `CompositeBackend` routing a single reference file to local disk, with a `FilesystemPermission` rule denying all writes to it. This homework asks you to do the same thing, but for a file-based task of your own choosing.

Open `typescript/m2/m2.2_homework.ts`.

Your choice here changes what actually happens when you run your file, compared to the lab:

| Backend | What happens to your file | Compared to the lab |
|---|---|---|
| `StateBackend` | Lives only in the thread's saved state; disappears once the thread ends, nothing touches disk | The lab wrote a real file to disk; this skips that entirely |
| `FilesystemBackend` (`virtual_mode=True`) | A real file on disk under whatever `root_dir` you set; still there after the script exits | Same real-disk behavior as the lab's `/reference/` route, but applied to your whole filesystem instead of one path |
| `CompositeBackend` | A real file on disk under `root_dir`, while everything else stays in thread state | Closest match to the lab's exact setup: one real path on disk, everything else in state |

Whichever you pick, `FilesystemPermission` rules behave the same: they're checked against the path before the backend is touched, so the same allow/deny/interrupt outcome happens no matter where the file actually lives.

1. **TODO 1**: pick a topic for a small text file (a packing list, a journal, a recipe box, meeting notes, etc.), seed it with some starting content, and configure any backend you like: `StateBackend`, `FilesystemBackend`, or a `CompositeBackend` routing between them.
2. **TODO 2**: write a task message that has the agent read your file, then write or edit it, and optionally add a `FilesystemPermission` rule that changes what the agent is allowed to do.

### Run it

```bash
cd typescript
pnpm tsx ./m2/m2.2_homework.ts
```

Stuck, or want to see the finished shape before you start? `m2.2_homework_filled.ts` is a reference copy with every TODO filled in (using a recipe box with one heirloom recipe locked against writes); run it the same way to see what "done" looks like end to end.

Try to avoid peeking unless you're really stuck, you'll get more out of this by working through the TODOs yourself first.

---

<details markdown="1" style="border:2.5px solid #7FC8FF;border-radius:6px;background:#000;margin:1rem 0;"><summary style="padding:15px 16px;display:flex;align-items:center;cursor:pointer;font-weight:500;font-family:'IBM Plex Mono',monospace;font-size:14px;color:#7FC8FF;">Optional Extension: Want To Code From Scratch?</summary><div style="padding:12px 16px 16px;background:#fff;color:#000;border-radius:0 0 4px 4px;" markdown="1">

**What you'll build:** a filesystem tool allowlist that hides `write_file` from the model entirely, instead of letting it call the tool and blocking the call with a permission rule.

**Requirements:**

- Write your own `createDeepAgent({ model, backend: ..., middleware: [createFilesystemMiddleware({ tools: [...] })] })` call from scratch, in a new file under `typescript/m2/`.
- Configure a `CompositeBackend` routing one directory to a `new FilesystemBackend({ rootDir: ..., virtualMode: true })` (same shape as the lab), and seed it with a starting file.
- Add a `createFilesystemMiddleware({ tools: [...] })` allowlist that leaves `write_file` (and/or `edit_file`) off the list, so the model never even sees that tool exists. `read_file` must stay on the list.
- Write a task that asks the agent to read the file, then also asks it to edit it. Check the trace/messages: no `write_file`/`edit_file` tool call shows up at all, since the model was never given that tool, unlike a `FilesystemPermission` deny rule, where the model calls the tool and only then gets blocked.

</div></details>

</div>

</div>

<script>
(function () {
  var tabs = document.querySelectorAll('.lt-tab');
  function show(p) {
    tabs.forEach(function (t) {
      var on = t.getAttribute('data-p') === p;
      t.classList.toggle('active', on);
      t.setAttribute('aria-selected', on ? 'true' : 'false');
    });
    document.querySelectorAll('.lt-panel').forEach(function (panel) {
      panel.classList.toggle('active', panel.id === 'p-' + p);
    });
  }
  tabs.forEach(function (t) {
    t.addEventListener('click', function () { show(t.getAttribute('data-p')); });
  });
})();
</script>
