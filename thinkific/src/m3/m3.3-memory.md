[For translation, open lesson in new tab and use Chrome translate](https://langchain-ai.github.io/lca-lessons/deepagents/m3/m3.3-memory.html)

<style>@import url('../shared/sd-components.css');</style>
<script src="../shared/sd-components.js"></script>

<div class="page-lang-switch" data-page-lang-switch>
  <button class="page-lang-btn" data-page-lang="python" aria-selected="false">Python</button>
  <button class="page-lang-btn" data-page-lang="typescript" aria-selected="false">TypeScript</button>
</div>

# Memory

<style>
.lt-bar {
  display: flex;
  flex-wrap: wrap;
  gap: 20px;
  margin: 28px 0 0;
  border-bottom: 2px solid #CCE9FF;
}
.lt-group { display: flex; gap: 3px; }
.lt-mem  { --c: #0891B2; }
.lt-wrap { --c: #B45309; }
.lt-homework { --c: #0E9F6E; }
.lt-tab {
  font: 500 14px 'IBM Plex Mono', monospace;
  padding: 9px 14px;
  border: none;
  background: transparent;
  color: #40668D;
  cursor: pointer;
  border-bottom: 3px solid transparent;
  margin-bottom: -2px;
  border-radius: 6px 6px 0 0;
  transition: background .15s, color .15s, border-color .15s;
  white-space: nowrap;
}
.lt-tab:hover { background: #F2FAFF; color: #030710; }
.lt-tab.active {
  color: var(--c);
  border-bottom-color: var(--c);
  background: #fff;
}
.lt-panel { display: none; padding-top: 24px; }
.lt-panel.active { display: block; }
@media (max-width: 600px) {
  .lt-bar { flex-wrap: nowrap; overflow-x: auto; gap: 12px; }
  .lt-tab { padding: 8px 10px; font-size: 13px; }
}
</style>

<div class="lt-bar" role="tablist" aria-label="Lesson sections">
  <div class="lt-group lt-mem">
    <button class="lt-tab active" data-p="mem" role="tab" aria-selected="true">Memory</button>
  </div>
  <div class="lt-group lt-wrap">
    <button class="lt-tab" data-p="lab" role="tab" aria-selected="false">Lab</button>
  </div>
  <div class="lt-group lt-quiz">
    <button class="lt-tab" data-p="quiz" role="tab" aria-selected="false">Quiz</button>
  </div>
  <div class="lt-group lt-homework">
    <button class="lt-tab" data-p="homework" role="tab" aria-selected="false">Homework</button>
  </div>
</div>

<div class="lt-panel active" id="p-mem" markdown="1" role="tabpanel">

<details style="border:2.5px solid #000;border-radius:6px;background:#fff;margin:1rem 0;"><summary style="padding:10px 16px;cursor:pointer;font-weight:500;font-family:'IBM Plex Mono',monospace;font-size:14px;">Lesson Video Walkthrough</summary><div style="padding:12px 16px 16px;"><div class="video-container" style="max-width:750px;"><div class="video-wrapper"><iframe src="https://share.descript.com/embed/JxDfJX9rj1l" frameborder="0" allow="autoplay; fullscreen; encrypted-media; picture-in-picture" allowfullscreen></iframe></div></div></div></details>

<div data-lang="python" markdown="1">

Agent memory splits into two scopes: short-term and long-term.

- **Short-term memory** is the running message history for a single thread. It's tracked automatically by the checkpointer and does not persist beyond that thread.
- **Long-term memory** is what the agent has learned that should carry over across threads: user preferences, project conventions, recurring facts. It's tracked in a persistent store or filesystem, separate from any one thread's message history.

The distinction is scope (rather than lifetime):

- A checkpointer can be backed by a real database and outlive the process (it's still short-term because that history only exists inside the thread that created it).
- Long-term memory is visible to any thread that matches its scope (same user, same workspace, whatever you choose), regardless of which thread wrote it.

Both scopes can technically live in the filesystem. By default, Deep Agents already offloads some short-term context to files, mainly to keep large tool outputs out of the message history and save context. What makes memory distinct is that it's long-term: scoped to persist across threads, not just within one.

This lesson focuses on long-term memory. In Deep Agents, long-term memory is represented as files in the backend, and any file the agent can read or write can be treated as memory once you configure it that way.

<iframe src="images/m3.3-memory-slides.html" style="width:100%;height:425px;border:none;border-radius:12px;display:block;margin:1.25rem 0 1.5rem;" title="Checkpointer vs. memory walkthrough; use arrow keys or Prev/Next to navigate"></iframe>

The checkpointer's job is to load that history back in when a run starts and save it again after each step, so the thread's memory persists from one run to the next. It isn't part of the conversation itself, it's what "remembers" it: the LLM only ever sees the message history for the current thread, and never talks to the checkpointer directly.

---

## The mental model: memory is files

<details style="border:2.5px solid #000;border-radius:6px;background:#fff;margin:1rem 0;"><summary style="padding:10px 16px;cursor:pointer;font-weight:500;font-family:'IBM Plex Mono',monospace;font-size:14px;">Lesson Video Walkthrough</summary><div style="padding:12px 16px 16px;"><div class="video-container" style="max-width:750px;"><div class="video-wrapper"><iframe src="https://share.descript.com/embed/MVBfa2AadOp" frameborder="0" allow="autoplay; fullscreen; encrypted-media; picture-in-picture" allowfullscreen></iframe></div></div></div></details>

In Deep Agents, memory is plain files on the agent filesystem. The common convention is a markdown file such as `/memories/AGENTS.md`.

<div style="border-radius:10px;overflow:hidden;background:#0b0f1a;border:1px solid #1c2438;margin:1rem 0;">
<div style="display:flex;align-items:center;gap:8px;padding:10px 14px;background:#141b2e;border-bottom:1px solid #1c2438;">
<span style="width:11px;height:11px;border-radius:50%;background:#FF5F56;display:inline-block;"></span>
<span style="width:11px;height:11px;border-radius:50%;background:#FFBD2E;display:inline-block;"></span>
<span style="width:11px;height:11px;border-radius:50%;background:#27C93F;display:inline-block;"></span>
<span style="font-family:'IBM Plex Mono',ui-monospace,monospace;font-size:13px;color:#8FA6C2;margin-left:6px;">AGENTS.md</span>
</div>
<pre style="margin:0;padding:18px 20px;background:transparent;overflow-x:auto;"><code style="font-family:'IBM Plex Mono',ui-monospace,monospace;font-size:14px;line-height:1.6;color:#E6EDF3;white-space:pre;"><span style="color:#7FC8FF;">#</span> Project Guidelines

<span style="color:#7FC8FF;">##</span> Code Style
<span style="color:#8FA6C2;">-</span> Use type annotations
<span style="color:#8FA6C2;">-</span> Prefer pathlib.Path for file operations

<span style="color:#7FC8FF;">##</span> Workflow
<span style="color:#8FA6C2;">-</span> Run tests with: uv run pytest</code></pre>
</div>

The agent can update memory with the same filesystem tools it already knows, especially `edit_file`. There is no separate memory database API for the agent to learn.

The `memory` argument tells Deep Agents which files should be treated as memory:

```python {3}
agent = create_deep_agent(
    model=model,
    memory=["/memories/AGENTS.md"],
)
```

Without `memory=[...]`, the file may still exist in a backend, but it is not loaded into the system prompt as agent memory.

---

## How memory is injected

Passing `memory=[...]` creates `MemoryMiddleware` behind the scenes.

For each run, the middleware:

1. Loads the configured memory files from the backend
2. Stores the loaded contents in agent state
3. Appends the combined memory to the system prompt inside an `<agent_memory>` block on each model call in that run

That means memory is different from skills:

| | Memory | Skills |
|---|---|---|
| What is always visible? | Loaded memory content | Skill names and descriptions |
| When is full content loaded? | Proactively for the run | On demand when the agent activates a skill |
| Typical use | Persistent facts and instructions | Reusable workflows and procedures |

Head to the **Lab** tab to see the `<agent_memory>` block in LangSmith.

---

## How memory is updated

The middleware also adds instructions telling the agent when to persist new information. When a user says "remember this" or provides context that should carry forward, the agent can call `edit_file` on one of the paths passed to `memory=[...]`. There's no separate, context-derived filename: the path is fixed by `memory=[...]`; only where that fixed path's contents are stored can vary.

That write updates the backend file. The new content is available after memory is reloaded in a later run.

A typical memory write looks like:

```text
User: Remember: the team switched to ruff for linting.
Agent: edit_file('/memories/AGENTS.md', ...)
```

Only store information that is safe and useful in future conversations. Do not store secrets such as API keys, credentials, tokens, or passwords.

---

## Where memory lives

The backend controls where memory files are stored. A common production pattern is to use a `CompositeBackend`: keep normal working files in the default backend, and route a dedicated `/memories/` directory to persistent storage.

```python
from deepagents.backends import CompositeBackend, StateBackend, StoreBackend
from langgraph.store.memory import InMemoryStore

store = InMemoryStore()


def memory_namespace(runtime):
    user_id = runtime.context["user_id"]
    workspace_id = runtime.context["workspace_id"]
    return ("memory", workspace_id, user_id)


agent = create_deep_agent(
    model=model,
    backend=CompositeBackend(
        default=StateBackend(),
        routes={"/memories/": StoreBackend(namespace=memory_namespace)},
    ),
    store=store,
    memory=["/memories/AGENTS.md"],
)
```

In this setup, `/memories/AGENTS.md` is the logical path the agent sees. The `CompositeBackend` routes that path into `StoreBackend`, so memory persists beyond a single thread. Other working files can still use the default backend.

`StateBackend` is thread-scoped, so it is useful for temporary working files but not for persistent long-term memory.

| Component | Scope | Persists past the thread? | Use for |
|---|---|---|---|
| `StateBackend` | Single thread | No | Temporary working files |
| `StoreBackend` | Namespace you define (e.g. workspace/user) | Yes | Persistent, long-term memory |
| `CompositeBackend` | N/A, routes paths to the backend above | Depends on the route | Splitting working files from memory files |

`InMemoryStore()` is not a backend itself; it is the `BaseStore` implementation that `StoreBackend` writes to, passed in via `store=`. It plays the same role for cross-thread memory that `InMemorySaver` plays for thread history: a non-persistent, in-process implementation for local development.

<mark class="hl">In production, you pass a persistent `store` (and checkpointer) backed by real infrastructure instead.</mark>

---

## Scoping memory

The `StoreBackend` namespace on the `/memories/` route determines which stored copy of that memory file the agent sees. In a real application, you usually derive that namespace from runtime context such as the authenticated user, workspace, tenant, or assistant ID.

Scope is not a fixed choice between "shared" and "per-user." It comes from which segments you include in the namespace tuple, and any combination is valid. Each combination is its own exact namespace tuple, not an overlap or subset of the others: `("memory", assistant_id)` and `("memory", assistant_id, user_id)` are unrelated keys in the store, even though one looks like a prefix of the other. This isn't something the agent mixes and matches per request either: you fix the tuple shape once, in the namespace factory, and every invocation resolves to that same single namespace.

The `memory_namespace` function on the `/memories/` route in the previous section already defines a user-and-workspace scoped namespace. Invoke the agent with matching context, and the store resolves to that same namespace:

```python
agent.invoke(
    {"messages": [{"role": "user", "content": "Remember that I prefer concise answers."}]},
    context={"user_id": "u_123", "workspace_id": "acme"},
)
```

Common scoping patterns:

| Scope | Namespace shape | Use when... |
|---|---|---|
| Shared assistant memory | `("memory", assistant_id)` | Everyone should share the same persistent instructions |
| User memory | `("memory", user_id)` | Preferences should follow one user across threads |
| Workspace memory | `("memory", workspace_id)` | A team/project should share conventions |
| User within workspace | `("memory", workspace_id, user_id)` | Users have private preferences inside a workspace |
| Assistant + user | `("memory", assistant_id, user_id)` | The same user may have different memories for different assistants |

Choose the scope deliberately. If private user memories share the same namespace, one user's preferences or context can leak into another user's agent run. If shared team memory is scoped too narrowly, the agent will fail to reuse conventions that should apply across the workspace.

---

## Recap

- Memory is not thread history; checkpointers preserve thread history
- Memory is persistent knowledge stored as files
- `memory=[...]` activates `MemoryMiddleware` for those files
- Loaded memory is injected into the system prompt inside `<agent_memory>`
- Agents update memory by editing memory files
- The `/memories/` backend route determines persistence; the StoreBackend namespace determines scope

---

## References

**Documentation:**

- [Memory (Deep Agents)](https://docs.langchain.com/oss/python/deepagents/memory)
- [Filesystem Backends](https://docs.langchain.com/oss/python/deepagents/backends)
- [agents.md specification](https://agents.md/)

</div>

<div data-lang="typescript" markdown="1">

Agent memory splits into two scopes: short-term and long-term.

- **Short-term memory** is the running message history for a single thread. It's tracked automatically by the checkpointer and does not persist beyond that thread.
- **Long-term memory** is what the agent has learned that should carry over across threads: user preferences, project conventions, recurring facts. It's tracked in a persistent store or filesystem, separate from any one thread's message history.

The distinction is scope (rather than lifetime):

- A checkpointer can be backed by a real database and outlive the process (it's still short-term because that history only exists inside the thread that created it).
- Long-term memory is visible to any thread that matches its scope (same user, same workspace, whatever you choose), regardless of which thread wrote it.

Both scopes can technically live in the filesystem. By default, Deep Agents already offloads some short-term context to files, mainly to keep large tool outputs out of the message history and save context. What makes memory distinct is that it's long-term: scoped to persist across threads, not just within one.

This lesson focuses on long-term memory. In Deep Agents, long-term memory is represented as files in the backend, and any file the agent can read or write can be treated as memory once you configure it that way.

<iframe src="images/m3.3-memory-slides.html" style="width:100%;height:425px;border:none;border-radius:12px;display:block;margin:1.25rem 0 1.5rem;" title="Checkpointer vs. memory walkthrough; use arrow keys or Prev/Next to navigate"></iframe>

The checkpointer's job is to load that history back in when a run starts and save it again after each step, so the thread's memory persists from one run to the next. It isn't part of the conversation itself, it's what "remembers" it: the LLM only ever sees the message history for the current thread, and never talks to the checkpointer directly.

---

## The mental model: memory is files

<details style="border:2.5px solid #000;border-radius:6px;background:#fff;margin:1rem 0;"><summary style="padding:10px 16px;cursor:pointer;font-weight:500;font-family:'IBM Plex Mono',monospace;font-size:14px;">Lesson Video Walkthrough</summary><div style="padding:12px 16px 16px;"><div class="video-container" style="max-width:750px;"><div class="video-wrapper"><iframe src="https://share.descript.com/embed/MVBfa2AadOp" frameborder="0" allow="autoplay; fullscreen; encrypted-media; picture-in-picture" allowfullscreen></iframe></div></div></div></details>

In Deep Agents, memory is plain files on the agent filesystem. The common convention is a markdown file such as `/memories/AGENTS.md`.

<div style="border-radius:10px;overflow:hidden;background:#0b0f1a;border:1px solid #1c2438;margin:1rem 0;">
<div style="display:flex;align-items:center;gap:8px;padding:10px 14px;background:#141b2e;border-bottom:1px solid #1c2438;">
<span style="width:11px;height:11px;border-radius:50%;background:#FF5F56;display:inline-block;"></span>
<span style="width:11px;height:11px;border-radius:50%;background:#FFBD2E;display:inline-block;"></span>
<span style="width:11px;height:11px;border-radius:50%;background:#27C93F;display:inline-block;"></span>
<span style="font-family:'IBM Plex Mono',ui-monospace,monospace;font-size:13px;color:#8FA6C2;margin-left:6px;">AGENTS.md</span>
</div>
<pre style="margin:0;padding:18px 20px;background:transparent;overflow-x:auto;"><code style="font-family:'IBM Plex Mono',ui-monospace,monospace;font-size:14px;line-height:1.6;color:#E6EDF3;white-space:pre;"><span style="color:#7FC8FF;">#</span> Project Guidelines

<span style="color:#7FC8FF;">##</span> Code Style
<span style="color:#8FA6C2;">-</span> Use camelCase for variables and functions
<span style="color:#8FA6C2;">-</span> Prefer path.join for file operations

<span style="color:#7FC8FF;">##</span> Workflow
<span style="color:#8FA6C2;">-</span> Run tests with: pnpm test</code></pre>
</div>

The agent can update memory with the same filesystem tools it already knows, especially `edit_file`. There is no separate memory database API for the agent to learn.

The `memory` option tells Deep Agents which files should be treated as memory:

```typescript {3}
const agent = createDeepAgent({
  model,
  memory: ["/memories/AGENTS.md"],
});
```

Without `memory: [...]`, the file may still exist in a backend, but it is not loaded into the system prompt as agent memory.

---

## How memory is injected

Passing `memory: [...]` creates a memory middleware behind the scenes.

For each run, the middleware:

1. Loads the configured memory files from the backend
2. Stores the loaded contents in agent state
3. Appends the combined memory to the system prompt inside an `<agent_memory>` block on each model call in that run

That means memory is different from skills:

| | Memory | Skills |
|---|---|---|
| What is always visible? | Loaded memory content | Skill names and descriptions |
| When is full content loaded? | Proactively for the run | On demand when the agent activates a skill |
| Typical use | Persistent facts and instructions | Reusable workflows and procedures |

Head to the **Lab** tab to see the `<agent_memory>` block in LangSmith.

---

## How memory is updated

The middleware also adds instructions telling the agent when to persist new information. When a user says "remember this" or provides context that should carry forward, the agent can call `edit_file` on one of the paths passed to `memory: [...]`. There's no separate, context-derived filename: the path is fixed by `memory: [...]`; only where that fixed path's contents are stored can vary.

That write updates the backend file. The new content is available after memory is reloaded in a later run.

A typical memory write looks like:

```text
User: Remember: the team switched to ruff for linting.
Agent: edit_file('/memories/AGENTS.md', ...)
```

Only store information that is safe and useful in future conversations. Do not store secrets such as API keys, credentials, tokens, or passwords.

---

## Where memory lives

The backend controls where memory files are stored. A common production pattern is to use a `CompositeBackend`: keep normal working files in the default backend, and route a dedicated `/memories/` directory to persistent storage.

```typescript
import {
  createDeepAgent,
  CompositeBackend,
  StateBackend,
  StoreBackend,
  type StoreBackendContext,
} from "deepagents";
import { InMemoryStore } from "@langchain/langgraph";

const store = new InMemoryStore();

function memoryNamespace(context: StoreBackendContext): string[] {
  const configurable = (context.config?.configurable ?? {}) as {
    workspace_id: string;
    user_id: string;
  };
  return ["memory", configurable.workspace_id, configurable.user_id];
}

const agent = createDeepAgent({
  model,
  backend: new CompositeBackend(new StateBackend(), {
    "/memories/": new StoreBackend({ namespace: memoryNamespace }),
  }),
  store,
  memory: ["/memories/AGENTS.md"],
});
```

In this setup, `/memories/AGENTS.md` is the logical path the agent sees. The `CompositeBackend` routes that path into `StoreBackend`, so memory persists beyond a single thread. Other working files can still use the default backend.

`StateBackend` is thread-scoped, so it is useful for temporary working files but not for persistent long-term memory.

| Component | Scope | Persists past the thread? | Use for |
|---|---|---|---|
| `StateBackend` | Single thread | No | Temporary working files |
| `StoreBackend` | Namespace you define (e.g. workspace/user) | Yes | Persistent, long-term memory |
| `CompositeBackend` | N/A, routes paths to the backend above | Depends on the route | Splitting working files from memory files |

`new InMemoryStore()` is not a backend itself; it is the store implementation that `StoreBackend` writes to, passed in via `store`. It plays the same role for cross-thread memory that an in-memory checkpointer plays for thread history: a non-persistent, in-process implementation for local development.

<mark class="hl">In production, you pass a persistent `store` (and checkpointer) backed by real infrastructure instead.</mark>

---

## Scoping memory

The `StoreBackend` namespace on the `/memories/` route determines which stored copy of that memory file the agent sees. In a real application, you usually derive that namespace from the run's `RunnableConfig`, such as the authenticated user, workspace, tenant, or assistant ID threaded through `configurable`.

Scope is not a fixed choice between "shared" and "per-user." It comes from which segments you include in the namespace array, and any combination is valid. Each combination is its own exact namespace key, not an overlap or subset of the others: `["memory", assistantId]` and `["memory", assistantId, userId]` are unrelated keys in the store, even though one looks like a prefix of the other. This isn't something the agent mixes and matches per request either: you fix the array shape once, in the namespace factory, and every invocation resolves to that same single namespace.

The `memoryNamespace` function on the `/memories/` route in the previous section already defines a user-and-workspace scoped namespace. Invoke the agent with matching context threaded through `configurable`, and the store resolves to that same namespace:

```typescript
await agent.invoke(
  { messages: [{ role: "user", content: "Remember that I prefer concise answers." }] },
  { configurable: { user_id: "u_123", workspace_id: "acme" } },
);
```

Common scoping patterns:

| Scope | Namespace shape | Use when... |
|---|---|---|
| Shared assistant memory | `["memory", assistantId]` | Everyone should share the same persistent instructions |
| User memory | `["memory", userId]` | Preferences should follow one user across threads |
| Workspace memory | `["memory", workspaceId]` | A team/project should share conventions |
| User within workspace | `["memory", workspaceId, userId]` | Users have private preferences inside a workspace |
| Assistant + user | `["memory", assistantId, userId]` | The same user may have different memories for different assistants |

Choose the scope deliberately. If private user memories share the same namespace, one user's preferences or context can leak into another user's agent run. If shared team memory is scoped too narrowly, the agent will fail to reuse conventions that should apply across the workspace.

---

## Recap

- Memory is not thread history; checkpointers preserve thread history
- Memory is persistent knowledge stored as files
- `memory: [...]` activates the memory middleware for those files
- Loaded memory is injected into the system prompt inside `<agent_memory>`
- Agents update memory by editing memory files
- The `/memories/` backend route determines persistence; the StoreBackend namespace determines scope

---

## References

**Documentation:**

- [Memory (Deep Agents)](https://docs.langchain.com/oss/javascript/deepagents/memory)
- [Filesystem Backends](https://docs.langchain.com/oss/javascript/deepagents/backends)
- [agents.md specification](https://agents.md/)

</div>

</div>

<div class="lt-panel" id="p-quiz" role="tabpanel">

<h2>Check your understanding</h2>

<MCQ
    question="What is the difference between checkpointers and memory?"
    choices='["Checkpointers preserve persistent user preferences; memory preserves messages", "Checkpointers preserve thread history/state; memory stores persistent file-backed knowledge", "They are two names for the same feature", "Memory is only for tool outputs; checkpointers are only for system prompts"]'
    correctIndex={1}
    explanation="Checkpointers preserve message history and state within a thread. Memory stores persistent facts and instructions as files that can be loaded into future runs, including different threads when the backend scope allows it."
/>

<MCQ
    question="What does memory=[&quot;/memories/AGENTS.md&quot;] do?"
    choices='["It activates MemoryMiddleware for that file so its contents can be loaded and injected as memory", "It creates a new checkpointer for that path", "It makes every file under /memories read-only", "It converts the file into a skill"]'
    correctIndex={0}
    explanation="The memory argument tells Deep Agents which files should be treated as memory. Without it, a file may still exist in the backend, but it is not loaded into the system prompt as agent memory."
/>

<MCQ
    question="Where can you confirm memory injection in a LangSmith trace?"
    choices='["In the &lt;agent_memory&gt; block at the bottom of the system message", "Only in a separate Memory tab", "As the first ToolMessage in every run", "Only after the agent explicitly calls read_file"]'
    correctIndex={0}
    explanation="Loaded memory content is appended to the system prompt inside an &lt;agent_memory&gt; block on model calls in the run."
/>

<MCQ
    question="How does an agent persist new information to memory?"
    choices='["By returning a special memory_update key in its final response", "By calling edit_file on one of the configured memory file paths", "By creating a new skill", "By writing directly to the checkpointer"]'
    correctIndex={1}
    explanation="Memory is file-backed. When the agent should remember something, it edits a configured memory file such as /memories/AGENTS.md. That write persists to the backend and is available after memory is reloaded."
/>

<MCQ
    question="Why use StoreBackend for memory?"
    choices='["To make memory files available through persistent backend storage and namespaces", "To disable edit_file", "To load skills only on demand", "To replace the system prompt"]'
    correctIndex={0}
    explanation="StoreBackend stores files in a key/value store under a namespace. In the common CompositeBackend pattern, routing `/memories/` to StoreBackend makes that directory persistent and scopeable by user, workspace, tenant, or assistant."
/>

<MCQ
    question="What is the main risk of choosing the wrong memory namespace?"
    choices='["The model will stop seeing tool schemas", "Private memory can be shared with the wrong user or workspace", "The file extension changes from .md to .json", "The agent can no longer use skills"]'
    correctIndex={1}
    explanation="The namespace controls which files the agent sees. If private memories share a namespace, one user's preferences or context can leak into another user's run."
/>

</div>

<div class="lt-panel" id="p-lab" markdown="1" role="tabpanel">

<div data-lang="python" markdown="1">

## Lab: Coding Assistant with Memory

Open `python/m3/m3.3_memory_agent.py` to see how memory is wired through `CompositeBackend`: normal working files use the default backend, while `/memories/` routes to `StoreBackend`. The script derives the memory namespace from runtime context, seeds `/memories/AGENTS.md` into an in-memory store, then runs the agent twice. The first invoke answers a question using the memory content; the second triggers a memory write.

<Tip>

<details style="border:2.5px solid #000;border-radius:6px;background:#fff;margin:1rem 0;"><summary style="padding:10px 16px;cursor:pointer;font-weight:500;font-family:'IBM Plex Mono',monospace;font-size:14px;">Lab Video Walkthrough</summary><div style="padding:12px 16px 16px;"><div class="video-container" style="max-width:750px;"><div class="video-wrapper"><iframe src="https://share.descript.com/embed/0rmWKU64h0G" frameborder="0" allow="autoplay; fullscreen; encrypted-media; picture-in-picture" allowfullscreen></iframe></div></div></div></details>

**[View this run here in LangSmith.](https://smith.langchain.com/public/75a62f82-3517-4f3b-a447-98b70c346560/r)** Notice the `<agent_memory>` block in the system prompt holding the loaded `/memories/AGENTS.md` content, and the agent answering directly from it.

**[View this run here in LangSmith.](https://smith.langchain.com/public/80dc2c14-f909-41cb-94c8-eefa3d99b7ad/r)** Notice the agent calling `edit_file` on `/memories/AGENTS.md` to add the note about switching to ruff for linting.

<details style="border:2.5px solid #000;border-radius:6px;background:#E5F4FF;margin:1rem 0;"><summary style="padding:10px 16px;cursor:pointer;font-weight:500;font-family:'IBM Plex Mono',monospace;font-size:14px;">LangSmith Video Walkthrough</summary><div style="padding:12px 16px 16px;"><div class="video-container" style="max-width:750px;"><div class="video-wrapper"><iframe src="https://share.descript.com/embed/cJ0X5fXAUah" frameborder="0" allow="autoplay; fullscreen; encrypted-media; picture-in-picture" allowfullscreen></iframe></div></div></div></details>

</Tip>

---

### Run it

Before running it, make sure your model API key from setup is available in the Python environment.

From the repo root:

```bash
cd python
uv run ./m3/m3.3_memory_agent.py
```

After both invokes complete, the script prints the stored `/memories/AGENTS.md` content from the store. Verify that the ruff note was written into that stored memory file.

</div>

<div data-lang="typescript" markdown="1">

## Lab: Coding Assistant with Memory

Open `typescript/m3/m3.3_memory_agent.ts` to see how memory is wired through `CompositeBackend`: normal working files use the default backend, while `/memories/` routes to `StoreBackend`. The script derives the memory namespace from the invoke call's `configurable`, seeds `/memories/AGENTS.md` into an in-memory store, then runs the agent twice. The first invoke answers a question using the memory content; the second triggers a memory write.


<Tip>

<details style="border:2.5px solid #000;border-radius:6px;background:#fff;margin:1rem 0;"><summary style="padding:10px 16px;cursor:pointer;font-weight:500;font-family:'IBM Plex Mono',monospace;font-size:14px;">Lab Video Walkthrough</summary><div style="padding:12px 16px 16px;"><div class="video-container" style="max-width:750px;"><div class="video-wrapper"><iframe src="https://share.descript.com/embed/0rmWKU64h0G" frameborder="0" allow="autoplay; fullscreen; encrypted-media; picture-in-picture" allowfullscreen></iframe></div></div></div></details>

**[View this run here in LangSmith.](https://smith.langchain.com/public/75a62f82-3517-4f3b-a447-98b70c346560/r)** Notice the `<agent_memory>` block in the system prompt holding the loaded `/memories/AGENTS.md` content, and the agent answering directly from it.

**[View this run here in LangSmith.](https://smith.langchain.com/public/80dc2c14-f909-41cb-94c8-eefa3d99b7ad/r)** Notice the agent calling `edit_file` on `/memories/AGENTS.md` to add the note about switching to ruff for linting.

<details style="border:2.5px solid #000;border-radius:6px;background:#E5F4FF;margin:1rem 0;"><summary style="padding:10px 16px;cursor:pointer;font-weight:500;font-family:'IBM Plex Mono',monospace;font-size:14px;">LangSmith Video Walkthrough</summary><div style="padding:12px 16px 16px;"><div class="video-container" style="max-width:750px;"><div class="video-wrapper"><iframe src="https://share.descript.com/embed/cJ0X5fXAUah" frameborder="0" allow="autoplay; fullscreen; encrypted-media; picture-in-picture" allowfullscreen></iframe></div></div></div></details>

</Tip>

---

### Run it

Before running it, make sure your model API key from setup is available in the environment.

From the repo root:

```bash
cd typescript
pnpm tsx ./m3/m3.3_memory_agent.ts
```

After both invokes complete, the script prints the stored `/memories/AGENTS.md` content from the store. Verify that the ruff note was written into that stored memory file.

</div>

</div>

<div class="lt-panel" id="p-homework" markdown="1" role="tabpanel">

<div data-lang="python" markdown="1">

## Prove Memory Isolation Between Users

The lab only ever ran the agent under one fixed workspace/user context. So isolation between users (the risk that private memories leak into another user's agent run when a namespace is scoped too broadly) was never actually demonstrated.

This homework closes that gap. Run the same agent under two different contexts that share a workspace but belong to different users, each resolving to its own `("memory", workspace_id, user_id)` namespace. Seed each context with different facts in a domain of your choosing. Then confirm a detail you tell the agent to remember under one context never leaks into what it says or stores under the other.

Open `python/m3/m3.3_homework.py`.

1. **TODO 1**: write two different seed memories, one for `CONTEXT_A` and one for `CONTEXT_B`, about the same general topic but with different specifics, so a leak would be obvious if it happened.
2. **TODO 2**: write three prompts: a question answerable from `CONTEXT_A`'s seed alone, a "remember this" message that adds a new, distinctive fact under `CONTEXT_A`, and the same question asked again under `CONTEXT_B` (it should get `CONTEXT_B`'s own answer, or no answer, never `CONTEXT_A`'s).

The script prints both contexts' final stored memory and flags whether isolation held.

### Run it

```bash
cd python
uv run ./m3/m3.3_homework.py
```

Stuck, or want to see the finished shape before you start? `m3.3_homework_filled.py` is a reference copy with every TODO filled in (using houseplant watering notes); run it the same way to see what "done" looks like end to end.

Try to avoid peeking unless you're really stuck, you'll get more out of this by working through the TODOs yourself first.

</div>

<div data-lang="typescript" markdown="1">

## Prove Memory Isolation Between Users

The lab only ever ran the agent under one fixed workspace/user context. So isolation between users (the risk that private memories leak into another user's agent run when a namespace is scoped too broadly) was never actually demonstrated.

This homework closes that gap. Run the same agent under two different contexts that share a workspace but belong to different users, each resolving to its own `("memory", workspace_id, user_id)` namespace. Seed each context with different facts in a domain of your choosing. Then confirm a detail you tell the agent to remember under one context never leaks into what it says or stores under the other.

Open `typescript/m3/m3.3_homework.ts`.

1. **TODO 1**: write two different seed memories, one for `CONTEXT_A` and one for `CONTEXT_B`, about the same general topic but with different specifics, so a leak would be obvious if it happened.
2. **TODO 2**: write three prompts: a question answerable from `CONTEXT_A`'s seed alone, a "remember this" message that adds a new, distinctive fact under `CONTEXT_A`, and the same question asked again under `CONTEXT_B` (it should get `CONTEXT_B`'s own answer, or no answer, never `CONTEXT_A`'s).

The script prints both contexts' final stored memory and flags whether isolation held.

### Run it

```bash
cd typescript
pnpm tsx ./m3/m3.3_homework.ts
```

Stuck, or want to see the finished shape before you start? `m3.3_homework_filled.ts` is a reference copy with every TODO filled in (using houseplant watering notes); run it the same way to see what "done" looks like end to end.

Try to avoid peeking unless you're really stuck, you'll get more out of this by working through the TODOs yourself first.

</div>

</div>

<script>
(function () {
  var tabs = document.querySelectorAll('.lt-tab');
  function show(p) {
    tabs.forEach(function (t) {
      var on = t.getAttribute('data-p') === p;
      t.classList.toggle('active', on);
      t.setAttribute('aria-selected', on ? 'true' : 'false');
    });
    document.querySelectorAll('.lt-panel').forEach(function (panel) {
      panel.classList.toggle('active', panel.id === 'p-' + p);
    });
  }
  tabs.forEach(function (t) {
    t.addEventListener('click', function () { show(t.getAttribute('data-p')); });
  });
})();
</script>
