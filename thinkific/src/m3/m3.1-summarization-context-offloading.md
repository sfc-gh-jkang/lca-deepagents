[For translation, open lesson in new tab and use Chrome translate](https://langchain-ai.github.io/lca-lessons/deepagents/m3/m3.1-summarization-context-offloading.html)

<style>@import url('../shared/sd-components.css');</style>
<script src="../shared/sd-components.js"></script>

<div class="page-lang-switch" data-page-lang-switch>
  <button class="page-lang-btn" data-page-lang="python" aria-selected="false">Python</button>
  <button class="page-lang-btn" data-page-lang="typescript" aria-selected="false">TypeScript</button>
</div>

# Summarization and Context Offloading


<style>
.lt-bar {
  display: flex;
  flex-wrap: wrap;
  gap: 20px;
  margin: 28px 0 0;
  border-bottom: 2px solid #CCE9FF;
}
.lt-group { display: flex; gap: 3px; }
.lt-quiz  { --c: #7C3AED; }
.lt-summ  { --c: #0369A1; }
.lt-homework { --c: #0E9F6E; }
.lt-tab {
  font: 500 14px 'IBM Plex Mono', monospace;
  padding: 9px 14px;
  border: none;
  background: transparent;
  color: #40668D;
  cursor: pointer;
  border-bottom: 3px solid transparent;
  margin-bottom: -2px;
  border-radius: 6px 6px 0 0;
  transition: background .15s, color .15s, border-color .15s;
  white-space: nowrap;
}
.lt-tab:hover { background: #F2FAFF; color: #030710; }
.lt-tab.active {
  color: var(--c);
  border-bottom-color: var(--c);
  background: #fff;
}
.lt-panel { display: none; padding-top: 24px; }
.lt-panel.active { display: block; }
@media (max-width: 600px) {
  .lt-bar { flex-wrap: nowrap; overflow-x: auto; gap: 12px; }
  .lt-tab { padding: 8px 10px; font-size: 13px; }
}
</style>

<div class="lt-bar" role="tablist" aria-label="Lesson sections">
  <div class="lt-group lt-summ">
    <button class="lt-tab active" data-p="summ" role="tab" aria-selected="true">Lesson</button>
  </div>
  <div class="lt-group lt-quiz">
    <button class="lt-tab" data-p="quiz" role="tab" aria-selected="false">Quiz</button>
  </div>
  <div class="lt-group lt-homework">
    <button class="lt-tab" data-p="homework" role="tab" aria-selected="false">Homework</button>
  </div>
</div>

<div class="lt-panel active" id="p-summ" markdown="1" role="tabpanel">

<details style="border:2.5px solid #000;border-radius:6px;background:#fff;margin:1rem 0;"><summary style="padding:10px 16px;cursor:pointer;font-weight:500;font-family:'IBM Plex Mono',monospace;font-size:14px;">Lesson Video Walkthrough</summary><div style="padding:12px 16px 16px;"><div class="video-container" style="max-width:750px;"><div class="video-wrapper"><iframe src="https://share.descript.com/embed/YARdGwwHje9" frameborder="0" allow="autoplay; fullscreen; encrypted-media; picture-in-picture" allowfullscreen></iframe></div></div></div></details>

<div style="display:flex;gap:20px;align-items:flex-start;">
  <img src="images/m3.1-summarization-left.svg" alt="Summarization: old messages below the keep cutoff are replaced by a Σ Summary block in the context window." style="flex:1;min-width:0;">
  <img src="images/m3.1-offloading-right.svg" alt="Offloading: large Tool Result and Human Message inputs are intercepted by FilesystemMiddleware, stored on the filesystem, and replaced with a preview pointer in the context window." style="flex:1;min-width:0;">
</div>

<br>

Long-running agents accumulate large message histories. Summarization works on messages in the context window and intelligently summarizes them. Offloading intercepts messages (human or tool messages) before they are inserted into context and saves them to the filesystem, inserting a pointer to the information in the context. These are detailed below.

---

## Summarization

### How the context window fills up

Think of the context window like a thermometer. Every new message adds tokens and the level rises. When it crosses **85%** of the model's limit, by default, the middleware fires.

<div class="sd-wrap" id="ss-summarization"></div>

<script>
buildSlideshow({
  id: 'ss-summarization',
  slides: [
    {
      src: 'images/m3.x-summarization.svg',
      tag: 'Slide 1 of 2',
      caption: `When the context hits 85%, the middleware picks a cutoff point that keeps the most recent messages intact and summarizes older messages into a single block. The messages being summarized are appended to /conversation_history/{thread_id}.md on the filesystem. The summary message the model receives includes a link to that file, so the agent can read those older sections back with read_file if it needs the detail. Because a summary is far more compact than the messages it replaces, the token count drops sharply and the conversation can continue.`
    },
    {
      src: 'images/m3.x-summarization2.svg',
      tag: 'Slide 2 of 2',
      caption: `On a second summarization event, the prior summary is treated like any other old message: it gets folded into a new combined summary along with whatever else has aged past the cutoff. There is no special handling for summaries; the same logic applies every time. This is what makes summarization scale indefinitely: each event produces one summary block, regardless of how many rounds have run before.`
    },
  ]
});
</script>

---

### What the summary contains

The middleware makes a separate LLM call to generate the summary. It uses a structured prompt that asks the model to extract:

- **Session intent**: what the user is trying to accomplish
- **Key facts & decisions**: important context from the conversation
- **Artifacts**: files or resources created or modified
- **Next steps**: what remains to do

The resulting text is injected as a single `HumanMessage` at the top of the preserved recent messages. The middleware also embeds a link to the saved older sections, so the agent can retrieve them with `read_file` if it needs the detail.

---

### Seeing it in action

<details style="border:2.5px solid #000;border-radius:6px;background:#fff;margin:1rem 0;"><summary style="padding:10px 16px;cursor:pointer;font-weight:500;font-family:'IBM Plex Mono',monospace;font-size:14px;">Lesson Video Walkthrough</summary><div style="padding:12px 16px 16px;"><div class="video-container" style="max-width:750px;"><div class="video-wrapper"><iframe src="https://share.descript.com/embed/jyMqNJVwvLd" frameborder="0" allow="autoplay; fullscreen; encrypted-media; picture-in-picture" allowfullscreen></iframe></div></div></div></details>

The default trigger is 85% of the model's real context window: 170,000 tokens for Claude Haiku 4.5.

As of deepagents 0.7:

- passing `trigger=("fraction", 0.5)` to `SummarizationMiddleware` sets the threshold to 50% instead of the default 85% (you can swap in any fraction you want).

To observe the default in a demo we set the model's reported context window to a small value so the threshold is hit after a few turns:

<div data-lang="python" markdown="1">

```python
# python/m3/m3.1_summarization.py
from models import model

# Lower the reported context window so summarization fires at ~595 tokens
# (85% of 700) instead of the real threshold. Must pass model object, not string.
model.profile = {**model.profile, "max_input_tokens": 700}

agent = create_deep_agent(
    model=model,
    checkpointer=MemorySaver(),
    system_prompt="You are a helpful assistant. Keep every response to one sentence.",
)
```

Because this demo uses separate `ainvoke()` calls, it relies on the runtime pattern from Module 1: a checkpointer plus the same `thread_id` lets message history accumulate across turns. Without either piece, there would be much less history to summarize.

From the repo root:

```bash
cd python
uv run python m3/m3.1_summarization.py
```

</div>
<div data-lang="typescript" markdown="1">


```typescript
// typescript/m3/m3.1_summarization.ts
import { createDeepAgent } from "deepagents";
import { MemorySaver } from "@langchain/langgraph";

import { model } from "../models.js";

// Shrink the reported context window so summarization triggers at ~340 tokens
// (85% of 400) instead of the real threshold. Must use model object, not string.
Object.defineProperty(model, "profile", {
  value: { ...model.profile, maxInputTokens: 400 },
  configurable: true,
});

const agent = createDeepAgent({
  model,
  checkpointer: new MemorySaver(),
  systemPrompt: "You are a helpful assistant. Keep every response to one sentence.",
});
```

Because this demo uses separate `agent.invoke()` calls, it relies on the runtime pattern from Module 1: a checkpointer plus the same `thread_id` lets message history accumulate across turns. Without either piece, there would be much less history to summarize.

From the repo root:

```bash
cd typescript
pnpm tsx ./m3/m3.1_summarization.ts
```

</div>

**Expected output:**

<pre style="max-height:260px;overflow-y:auto;background:#0d1117;color:#e6edf3;padding:16px;border-radius:6px;font-size:12px;font-family:'IBM Plex Mono',monospace;line-height:1.6;white-space:pre-wrap;">──────────────────────────────────────────────────
Turn 1  User:  My name is Alex. I work at Acme Corp.
Turn 1  Agent: Hello Alex, it's nice to meet you - welcome from Acme Corp!
  stored : 2 message(s) (raw history, never trimmed)

──────────────────────────────────────────────────
Turn 2  User:  What is 2 + 2?
Turn 2  Agent: 2 + 2 equals 4.
  stored : 4 message(s) (raw history, never trimmed)

──────────────────────────────────────────────────
Turn 3  User:  I have been building a distributed cache for three months.
Turn 3  Agent: That sounds like a substantial project - distributed caches are complex but rewarding to build!
  stored : 6 message(s) (raw history, never trimmed)
  model saw : summary + messages[4:]  [SUMMARIZED]

──────────────────────────────────────────────────
Turn 4  User:  What is the capital of France?
Turn 4  Agent: The capital of France is Paris.
  stored : 8 message(s) (raw history, never trimmed)
  model saw : summary + messages[4:]  [SUMMARIZED]

──────────────────────────────────────────────────
Turn 5  User:  What do you remember about me?
Turn 5  Agent: You mentioned your name is Alex, you work at Acme Corp, and you've been building a distributed cache for three months.
  stored : 10 message(s) (raw history, never trimmed)
  model saw : summary + messages[4:]  [SUMMARIZED]</pre>

<div data-lang="python" markdown="1">
<Tip>

**Passing `model` not `MODEL`.** Notice `create_deep_agent(model=model, ...)` receives the model *object*, not the string `"anthropic:claude-haiku-4-5"`. Passing the string would re-initialize the model internally and ignore the `profile` override.
</Tip>
</div>
<div data-lang="typescript" markdown="1">
<Tip>

**Passing `model` not `MODEL`.** Notice `createDeepAgent({ model, ... })` receives the model *object*, not the string `"anthropic:claude-haiku-4-5"`. Passing the string would re-initialize the model internally and ignore the `Object.defineProperty` override above.
</Tip>
</div>

<div data-lang="python" markdown="1">
When summarization fires, a private `_summarization_event` key appears in the agent's state. In the normal summarization path, the raw `state["messages"]` history is not replaced by the summary; summarization changes what gets *sent* to the model each turn.
</div>
<div data-lang="typescript" markdown="1">
When summarization fires, a private `_summarizationEvent` key appears in the agent's state. In the normal summarization path, the raw `state.messages` history is not replaced by the summary; summarization changes what gets *sent* to the model each turn.
</div>

---

## Offloading

<details style="border:2.5px solid #000;border-radius:6px;background:#fff;margin:1rem 0;"><summary style="padding:10px 16px;cursor:pointer;font-weight:500;font-family:'IBM Plex Mono',monospace;font-size:14px;">Lesson Video Walkthrough</summary><div style="padding:12px 16px 16px;"><div class="video-container" style="max-width:750px;"><div class="video-wrapper"><iframe src="https://share.descript.com/embed/DBjUzgBYFJG" frameborder="0" allow="autoplay; fullscreen; encrypted-media; picture-in-picture" allowfullscreen></iframe></div></div></div></details>

Summarization compacts messages that have already accumulated in context. Offloading handles a different case: a single large input. Instead of letting it land in context, **FilesystemMiddleware** intercepts oversized content before it reaches the model. It writes the full data to the filesystem and leaves behind a short preview plus a file path. The agent reads the full content with `read_file` only when it needs it.

Two kinds of data are offloaded this way: large **tool results** and large **human messages**.

### Tool result offloading

A single tool call can return a large result, such as a full database export, hundreds of search matches, or a long code generation output. Storing it inline would use a large share of the context window at once.

**FilesystemMiddleware** handles this automatically with tool result offloading. When a tool returns a result that exceeds roughly **20,000 tokens**, the middleware:

1. Writes the full result to `/large_tool_results/<tool_call_id>` on the agent's filesystem
2. Replaces the inline `ToolMessage` with a short preview (first and last 5 lines) and a file reference

The message the model actually sees looks like this:

```
Tool result too large, the result of this tool call <id> was saved in the
filesystem at this path: /large_tool_results/<tool_call_id>

You can read the result from the filesystem by using the read_file tool,
but make sure to only read part of the result at a time.

Here is a preview showing the head and tail of the result:

     1  | SELECT id, name, email FROM users WHERE ...
     2  | 1, Alice, alice@example.com
     3  | 2, Bob, bob@example.com
     4  | 3, Carol, carol@example.com
     5  | 4, Dave, dave@example.com
... [1 823 lines truncated] ...
  1828  | 1827, Zara, zara@example.com
  1829  | 1828, Zoe, zoe@example.com
  1830  | 1829, ...
```

The agent can then call `read_file` with `offset` and `limit` to page through the full result on demand - reading only the portion that is relevant rather than loading everything into context at once.

The tools `ls`, `glob`, `grep`, `read_file`, `edit_file`, and `write_file` are excluded from offloading. They either have their own built-in truncation, return minimal confirmation text, or (for `read_file`) re-reading a truncated file would not help.

### Human message offloading

The same mechanism applies when a *user* message is very large, for example pasting in a long document or transcript. When a human message exceeds roughly **50,000 tokens**, FilesystemMiddleware writes the full text to `/conversation_history/` and replaces it in the model's view with a preview and a file reference. The agent can `read_file` that path to retrieve the full message on demand.

The behavior the agent sees, a preview plus a pointer, is identical to a tool result. Only the threshold (50k vs 20k tokens) and the storage location differ.

Offloaded tool results and conversation history are written through whatever `backend=` the agent already has. The default `StateBackend` keeps them thread-scoped; route `default=` to a real disk or store backend via `CompositeBackend` to share them across threads.

<div class="case-study" markdown="1">
<img src="images/langchain-symbol.png" alt="LangChain" />

<div markdown="1">

**LangChain Blog**

[How Harmonic rebuilt Scout on Deep Agents](https://www.langchain.com/blog/how-harmonic-rebuilt-scout-on-deep-agents-and-4xd-retention-with-langsmith)

Harmonic rebuilt its Scout agent on filesystem-based context offloading, writing large results to disk and paging them back in on demand. Week one-to-four retention jumped 4x, and average session length increased 10x.

</div>
</div>

---

## Recap

<div data-lang="python" markdown="1">
- **SummarizationMiddleware** intercepts every LLM request via `wrap_model_call`; it runs before the model sees any messages
- When total tokens exceed 85% of `max_input_tokens` (the default; configurable via `trigger`), old messages are partitioned at the **keep cutoff** and replaced with a summary
- The summary is generated by a separate LLM call using the same model as the agent
- In the normal summarization path, raw `state["messages"]` is preserved and summarization is tracked in a private `_summarization_event` field
- Passing `model.profile["max_input_tokens"]` overrides the threshold, making it practical to demo
- **FilesystemMiddleware** also offloads individual tool results that exceed ~20k tokens, writing them to `/large_tool_results/` and replacing them with a preview + file reference
- The same offloading applies to oversized **human messages** (over ~50k tokens), written to `/conversation_history/` with a preview + file reference
</div>
<div data-lang="typescript" markdown="1">
- **SummarizationMiddleware** intercepts every LLM request via `wrapModelCall`; it runs before the model sees any messages
- When total tokens exceed 85% of `maxInputTokens` (the default; configurable via `trigger`), old messages are partitioned at the **keep cutoff** and replaced with a summary
- The summary is generated by a separate LLM call using the same model as the agent
- In the normal summarization path, raw `state.messages` is preserved and summarization is tracked in a private `_summarizationEvent` field
- Overriding `model.profile` with `Object.defineProperty` (a plain assignment won't work) lets you lower the threshold, making it practical to demo
- **FilesystemMiddleware** also offloads individual tool results that exceed ~20k tokens, writing them to `/large_tool_results/` and replacing them with a preview + file reference
- The same offloading applies to oversized **human messages** (over ~50k tokens), written to `/conversation_history/` with a preview + file reference
</div>

## Next

Complete the **Quiz** above, then continue to the next lesson on skills: how to package reusable agent behaviours so the model can load them on demand.

---

## References

<div data-lang="python" markdown="1">

**Documentation:**
- [SummarizationMiddleware: Deep Agents](https://docs.langchain.com/oss/python/deepagents/context-engineering)
- [Context Engineering overview](https://docs.langchain.com/oss/python/deepagents/context-engineering)
</div>
<div data-lang="typescript" markdown="1">

**Documentation:**
- [SummarizationMiddleware: Deep Agents](https://docs.langchain.com/oss/javascript/deepagents/context-engineering)
- [Context Engineering overview](https://docs.langchain.com/oss/javascript/deepagents/context-engineering)
</div>

</div>

<div class="lt-panel" id="p-quiz" role="tabpanel">

<h2>Check your understanding</h2>

<div data-lang="python">

<MCQ
    question="At what threshold does SummarizationMiddleware fire by default?"
    choices='["85% of the model&#39;s max_input_tokens", "50% of the model&#39;s max_input_tokens", "When the context window is completely full", "After a fixed count of 20 messages"]'
    correctIndex={0}
    explanation="SummarizationMiddleware fires when total tokens exceed 85% of max_input_tokens by default. For Claude Haiku 4.5 that is 170,000 tokens. That default is configurable via the trigger argument; overriding model.profile[&quot;max_input_tokens&quot;] is just a way to lower the threshold for demos."
/>

<MCQ
    question="How is the summary itself generated when SummarizationMiddleware fires?"
    choices='["By truncating the oldest messages until the token count drops below the limit", "By a fixed template that extracts the first sentence of each message", "By a separate LLM call using the same model as the agent", "By the agent model as part of generating its next response"]'
    correctIndex={2}
    explanation="The middleware makes its own dedicated LLM call, separate from the agent's response turn, using the same underlying model. It sends the old messages to that call with a structured prompt to extract session intent, key facts, artifacts, and next steps."
/>

<MCQ
    question="Where in the message list does the generated summary appear?"
    choices='["Appended to the system prompt as an additional instruction block", "As the very first message in state[\"messages\"]", "As a ToolMessage returned by a built-in summarize tool", "As a HumanMessage injected at the top of the preserved recent messages"]'
    correctIndex={3}
    explanation="The summary text is injected as a single HumanMessage placed at the top of the recent messages that were kept. The model receives the summary followed by the recent history."
/>

<MCQ
    question="What happens to state[&quot;messages&quot;] when summarization fires?"
    choices='["Older messages are deleted in place and replaced by the summary message", "In the normal summarization path, raw state[&quot;messages&quot;] is preserved; summarization affects what is sent to the model each turn", "state[&quot;messages&quot;] is cleared and rebuilt from the summary on every subsequent turn", "A new summarized state replaces the original, and the old state is discarded"]'
    correctIndex={1}
    explanation="In the normal summarization path, raw state[&quot;messages&quot;] is preserved and summarization is tracked separately. This is intentional: summarization is lossy, so keeping the raw history means you can audit what the agent said and did. Summarization affects what is sent to the model each turn, not the stored raw history."
/>

<MCQ
    question="Why must you pass the model object to create_deep_agent when using a profile override, rather than the model ID string?"
    choices='["The string form does not support multimodal inputs required for summarization", "String initialization disables SummarizationMiddleware entirely", "Passing the string re-initializes the model internally and discards the profile override", "The profile attribute only exists on string-initialized models, not objects"]'
    correctIndex={2}
    explanation="When you pass the model ID string, create_deep_agent re-initializes the model from scratch, which ignores any profile changes you made on the object. Passing the already-configured model object is the only way to ensure the overridden max_input_tokens reaches the middleware."
/>

</div>
<div data-lang="typescript">

<MCQ
    question="At what threshold does SummarizationMiddleware fire by default?"
    choices='["85% of the model&#39;s maxInputTokens", "50% of the model&#39;s maxInputTokens", "When the context window is completely full", "After a fixed count of 20 messages"]'
    correctIndex={0}
    explanation="SummarizationMiddleware fires when total tokens exceed 85% of maxInputTokens by default. For Claude Haiku 4.5 that is 170,000 tokens. That default is configurable via the trigger argument; overriding model.profile with Object.defineProperty (a plain assignment to model.profile does not work) is just a way to lower the threshold for demos."
/>

<MCQ
    question="How is the summary itself generated when SummarizationMiddleware fires?"
    choices='["By truncating the oldest messages until the token count drops below the limit", "By a fixed template that extracts the first sentence of each message", "By a separate LLM call using the same model as the agent", "By the agent model as part of generating its next response"]'
    correctIndex={2}
    explanation="The middleware makes its own dedicated LLM call, separate from the agent's response turn, using the same underlying model. It sends the old messages to that call with a structured prompt to extract session intent, key facts, artifacts, and next steps."
/>

<MCQ
    question="Where in the message list does the generated summary appear?"
    choices='["Appended to the system prompt as an additional instruction block", "As the very first message in state.messages", "As a ToolMessage returned by a built-in summarize tool", "As a HumanMessage injected at the top of the preserved recent messages"]'
    correctIndex={3}
    explanation="The summary text is injected as a single HumanMessage placed at the top of the recent messages that were kept. The model receives the summary followed by the recent history."
/>

<MCQ
    question="What happens to state.messages when summarization fires?"
    choices='["Older messages are deleted in place and replaced by the summary message", "In the normal summarization path, raw state.messages is preserved; summarization affects what is sent to the model each turn", "state.messages is cleared and rebuilt from the summary on every subsequent turn", "A new summarized state replaces the original, and the old state is discarded"]'
    correctIndex={1}
    explanation="In the normal summarization path, raw state.messages is preserved and summarization is tracked separately. This is intentional: summarization is lossy, so keeping the raw history means you can audit what the agent said and did. Summarization affects what is sent to the model each turn, not the stored raw history."
/>

<MCQ
    question="Why must you pass the model object to createDeepAgent when using a profile override, rather than the model ID string?"
    choices='["The string form does not support multimodal inputs required for summarization", "String initialization disables SummarizationMiddleware entirely", "Passing the string re-initializes the model internally and discards the profile override", "The profile property only exists on string-initialized models, not objects"]'
    correctIndex={2}
    explanation="When you pass the model ID string, createDeepAgent re-initializes the model from scratch, which ignores the Object.defineProperty override you applied to the object. Passing the already-configured model object is the only way to ensure the overridden maxInputTokens reaches the middleware."
/>

</div>


</div>

<div class="lt-panel" id="p-homework" markdown="1" role="tabpanel">

<div data-lang="python" markdown="1">

## Trigger Chained Summarization

In the lesson, you saw `SummarizationMiddleware` compress a short demo conversation once the context window filled past 85%. But summarization doesn't just fire once and stop: on a long enough conversation it fires again, each time re-summarizing on top of the previous summary, while the full evicted history keeps accumulating in a single backend file at `/conversation_history/{thread_id}.md`.

This homework asks you to build a conversation of your own choosing that's long enough to trigger summarization **at least twice**. Confirm the model can still recall a detail from your very first turn after being compacted multiple times and confirm the conversation history file actually accumulated multiple "Summarized at ..." sections.

Open `python/m3/m3.1_homework.py`.

1. **TODO 1**: write your own list of at least 8 user turns about a topic you pick. Put a concrete detail in one of your first two turns, keep talking about the topic for several more turns, then end with a turn that asks the agent to recall that early detail.
2. **TODO 2**: choose `model.profile["max_input_tokens"]` so that summarization fires **at least twice** before your last turn, not just once.

Note: if the agent calls `read_file` on its own conversation history mid-run, you may see a blank or duplicated "Summarized at ..." section; that's expected model behavior, not a bug in your code.

### Run it

```bash
cd python
uv run ./m3/m3.1_homework.py
```

Stuck, or want to see the finished shape before you start? `m3.1_homework_filled.py` is a reference copy with every TODO filled in (using a backyard-vegetable-garden planning conversation); run it the same way to see what "done" looks like end to end.

Try to avoid peeking unless you're really stuck, you'll get more out of this by working through the TODOs yourself first.

</div>
<div data-lang="typescript" markdown="1">

## Trigger Chained Summarization

In the lesson, you saw `SummarizationMiddleware` compress a short demo conversation once the context window filled past 85%. But summarization doesn't just fire once and stop: on a long enough conversation it fires again, each time re-summarizing on top of the previous summary, while the full evicted history keeps accumulating in a single backend file at `/conversation_history/{thread_id}.md`.

This homework asks you to build a conversation of your own choosing that's long enough to trigger summarization **at least twice**. Confirm the model can still recall a detail from your very first turn after being compacted multiple times and confirm the conversation history file actually accumulated multiple "Summarized at ..." sections.

Open `typescript/m3/m3.1_homework.ts`.

1. **TODO 1**: write your own list of at least 8 user turns about a topic you pick. Put a concrete detail in one of your first two turns, keep talking about the topic for several more turns, then end with a turn that asks the agent to recall that early detail.
2. **TODO 2**: choose `model.profile.maxInputTokens` (set with `Object.defineProperty`) so that summarization fires **at least twice** before your last turn, not just once.

Note: if the agent calls `read_file` on its own conversation history mid-run, you may see a blank or duplicated "Summarized at ..." section; that's expected model behavior, not a bug in your code.

### Run it

```bash
cd typescript
pnpm tsx ./m3/m3.1_homework.ts
```

Stuck, or want to see the finished shape before you start? `m3.1_homework_filled.ts` is a reference copy with every TODO filled in (using a backyard-vegetable-garden planning conversation); run it the same way to see what "done" looks like end to end.

Try to avoid peeking unless you're really stuck, you'll get more out of this by working through the TODOs yourself first.

</div>

</div>

<script>
(function () {
  var tabs = document.querySelectorAll('.lt-tab');
  function show(p) {
    tabs.forEach(function (t) {
      var on = t.getAttribute('data-p') === p;
      t.classList.toggle('active', on);
      t.setAttribute('aria-selected', on ? 'true' : 'false');
    });
    document.querySelectorAll('.lt-panel').forEach(function (panel) {
      panel.classList.toggle('active', panel.id === 'p-' + p);
    });
  }
  tabs.forEach(function (t) {
    t.addEventListener('click', function () { show(t.getAttribute('data-p')); });
  });
})();
</script>
